---
phase: 06-two-pass-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli_test.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-03

must_haves:
  truths:
    - "All 8 existing cli_test.py tests pass after analyze_job_description is wired into cli.py"
    - "New test verifies 'Analyzing job description...' progress message is printed"
    - "New test verifies generate_tailored_resume receives analysis=None when analysis returns None"
    - "New test verifies generate_tailored_resume receives the analysis dict when analysis succeeds"
  artifacts:
    - path: "src/cli_test.py"
      provides: "All 8 existing tests patched with @patch('cli.analyze_job_description') + 3 new tests"
      contains: "mock_analyze"
  key_links:
    - from: "src/cli_test.py"
      to: "src/cli.py"
      via: "@patch('cli.analyze_job_description', return_value=None)"
      pattern: "patch.*cli.analyze_job_description"
---

<objective>
Update all 8 existing tests in src/cli_test.py to add @patch("cli.analyze_job_description") — preventing them from calling real Ollama after cli.py is wired in Wave 3. Also add 3 new tests covering the new two-pass behavior.

Purpose: Protects the entire cli_test.py suite from breaking when cli.py gains the analyze_job_description() call. These tests start passing immediately after Wave 3 cli.py wiring completes.

Output: src/cli_test.py with all 8 existing tests patched + 3 new tests added.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/phases/06-two-pass-pipeline/06-CONTEXT.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-PATTERNS.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch all 8 existing cli_test.py tests with @patch("cli.analyze_job_description")</name>
  <files>src/cli_test.py</files>
  <read_first>
    - src/cli_test.py — read the entire file to see all 8 existing test functions, their current decorator stacks, and function signatures before making any changes
    - .planning/phases/06-two-pass-pipeline/06-PATTERNS.md — cli_test.py modifications section: exact decorator placement rule (outermost = last parameter), updated function signatures, @patch("cli.analyze_job_description", return_value=None) added as outermost decorator
    - .planning/phases/06-two-pass-pipeline/06-RESEARCH.md — Pitfall 1: all 8 tests must be patched; Patching section showing the decorator order rule
  </read_first>
  <action>
    Edit src/cli_test.py. For each of the 8 existing test functions, add @patch("cli.analyze_job_description", return_value=None) as the OUTERMOST @patch decorator (topmost line in the decorator stack, just above any @patch("sys.argv") line). Add mock_analyze as the LAST parameter in the function signature.

    Decorator order rule: Python applies decorators bottom-up, so the outermost (topmost) @patch maps to the LAST function parameter. The existing decorators and parameters remain unchanged in their relative order — only the new decorator is inserted at the top and mock_analyze appended at the end of the parameter list.

    Affected tests (all 8):
    1. test_end_sentinel_breaks_loop — currently has 6 params ending in mock_show_diff; add mock_analyze as param 7
    2. test_eof_treated_as_submission — currently has 6 params ending in mock_show_diff; add mock_analyze as param 7
    3. test_empty_jd_exits_1 — currently has 2 params (mock_input, mock_read); this test has no mock_generate/mock_write/mock_show_diff — still add the analyze patch as outermost, add mock_analyze as last param
    4. test_runtime_error_from_llm_exits_1 — currently has 4 params ending in mock_generate; add mock_analyze as param 5
    5. test_value_error_from_llm_exits_1 — currently has 4 params ending in mock_generate; add mock_analyze as param 5
    6. test_progress_message_printed — currently has 6 params ending in mock_show_diff; add mock_analyze as param 7
    7. test_success_prints_absolute_path — currently has 6 params ending in mock_show_diff; add mock_analyze as param 7
    8. test_show_diff_called_with_resume_and_tailored — currently has 6 params ending in mock_show_diff; add mock_analyze as param 7

    Default return_value=None on all 8 patches (simulates fallback path — the analysis call "failed" silently, pipeline continues with analysis=None). No existing assertion body needs to change.

    Also add import: from jd_analyzer import analyze_job_description is NOT needed in cli_test.py — the patch is on the cli module's reference. No new imports required.
  </action>
  <verify>
    <automated>cd /workspace && python -m py_compile src/cli_test.py && grep -c "cli.analyze_job_description" src/cli_test.py</automated>
  </verify>
  <done>
    src/cli_test.py compiles without syntax errors. grep -c "cli.analyze_job_description" src/cli_test.py returns 8 (one patch per existing test). All 8 existing test functions have mock_analyze as their final parameter.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3 new two-pass behavior tests to src/cli_test.py</name>
  <files>src/cli_test.py</files>
  <read_first>
    - src/cli_test.py — read the current state after Task 1 to understand the updated structure before appending
    - .planning/phases/06-two-pass-pipeline/06-PATTERNS.md — cli_test.py section: three new tests (progress message, analysis=None forwarding, analysis dict forwarding), printed_lines capture pattern at lines 117-122
    - .planning/phases/06-two-pass-pipeline/06-RESEARCH.md — Patching section showing how to assert mock_generate.call_args.kwargs["analysis"]
  </read_first>
  <behavior>
    - test_analyzing_progress_message_printed: assert "Analyzing job description..." appears in printed_lines before "Tailoring resume" appears; both messages must be present and in that order
    - test_generate_called_with_analysis_none_when_analysis_fails: set @patch("cli.analyze_job_description", return_value=None); call main(); assert mock_generate.call_args.kwargs["analysis"] is None
    - test_generate_called_with_analysis_dict_when_analysis_succeeds: set @patch("cli.analyze_job_description", return_value={"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}); call main(); assert mock_generate.call_args.kwargs["analysis"] == {"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}
  </behavior>
  <action>
    Append 3 new test functions to the end of src/cli_test.py. All 3 use the full standard decorator stack from the existing tests (sys.argv patch, show_diff, write_resume, generate_tailored_resume, read_resume, input, run_guards) plus @patch("cli.analyze_job_description") at the outermost position. All have mock_analyze as last parameter.

    test_analyzing_progress_message_printed: use the printed_lines capture pattern (patch builtins.print with side_effect lambda that appends to printed_lines list). Assert both "Analyzing job description..." and "Tailoring resume" appear. Assert "Analyzing job description..." index is less than "Tailoring resume" index in printed_lines — order matters per D-03.

    test_generate_called_with_analysis_none_when_analysis_fails: set @patch("cli.analyze_job_description", return_value=None). After main() runs, assert mock_generate.call_args.kwargs["analysis"] is None. This verifies the fallback path: when analysis returns None, generate_tailored_resume is called with analysis=None.

    test_generate_called_with_analysis_dict_when_analysis_succeeds: set @patch("cli.analyze_job_description", return_value={"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}). After main() runs, assert mock_generate.call_args.kwargs["analysis"] equals that exact dict. This verifies the injection path: when analysis returns a valid dict, generate_tailored_resume receives it as analysis keyword argument.

    Standard mock setup for all 3 new tests: mock_input.side_effect = ["Software Engineer job", "END"]; mock_read.return_value = "resume text"; mock_generate.return_value = TailorResult(content="\\documentclass{article}\n\\end{document}", fences_stripped=False); mock_write.return_value = Path("/tmp/tailored_resume_test.tex").
  </action>
  <verify>
    <automated>cd /workspace && python -m py_compile src/cli_test.py && grep -c "def test_" src/cli_test.py</automated>
  </verify>
  <done>
    src/cli_test.py compiles without syntax errors. grep -c "def test_" src/cli_test.py returns 11 (8 original + 3 new). The 3 new functions are test_analyzing_progress_message_printed, test_generate_called_with_analysis_none_when_analysis_fails, test_generate_called_with_analysis_dict_when_analysis_succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→module | cli_test.py patches cli module imports; no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | mock_generate.call_args.kwargs access | accept | KeyError on missing kwarg would surface as test failure, not silent data corruption |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
python -m py_compile src/cli_test.py exits 0
grep -c "cli.analyze_job_description" src/cli_test.py returns 11 (8 existing + 3 new)
grep -c "def test_" src/cli_test.py returns 11
</verification>

<success_criteria>
All 8 existing tests have @patch("cli.analyze_job_description", return_value=None) as outermost decorator and mock_analyze as final parameter. Three new tests cover: progress message order, analysis=None forwarding, analysis dict forwarding. File is syntactically valid.
</success_criteria>

<output>
Create .planning/phases/06-two-pass-pipeline/06-02-SUMMARY.md when done.
</output>
