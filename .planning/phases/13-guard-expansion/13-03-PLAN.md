---
phase: 13-guard-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/guards.py
  - tests/unit/test_guards.py
autonomous: true
requirements:
  - GARD-07
must_haves:
  truths:
    - "run_guards() calls _check_technology_substitution(original_text, tailored_text)"
    - "run_guards() calls _check_protected_sections(original_text, tailored_text)"
    - "_check_technology_substitution warnings surface when called via run_guards()"
    - "_check_protected_sections warnings surface when called via run_guards()"
    - "Neither new guard raises when called via run_guards() with None inputs"
    - "All twelve existing unit tests in tests/unit/test_guards.py continue to pass"
  artifacts:
    - path: "src/guards.py"
      provides: "run_guards() wired with both new guards"
      contains: "_check_technology_substitution(original_text, tailored_text)"
    - path: "src/guards.py"
      provides: "run_guards() wired with both new guards"
      contains: "_check_protected_sections(original_text, tailored_text)"
    - path: "tests/unit/test_guards.py"
      provides: "GARD-07 integration tests"
      contains: "test_run_guards_calls_technology_substitution"
  key_links:
    - from: "src/guards.py run_guards()"
      to: "_check_technology_substitution"
      via: "direct call inside run_guards body"
      pattern: "_check_technology_substitution\\(original_text"
    - from: "src/guards.py run_guards()"
      to: "_check_protected_sections"
      via: "direct call inside run_guards body"
      pattern: "_check_protected_sections\\(original_text"
---

<objective>
Wire `_check_technology_substitution` and `_check_protected_sections` into `run_guards()` in `src/guards.py` (GARD-07), and add two unit tests to `tests/unit/test_guards.py` verifying both guards are invoked by `run_guards()`.

Purpose: Plans 01 and 02 implemented and tested the two new guards in isolation. Plan 03 completes the integration by adding both call sites to `run_guards()` and proving via unit tests that `run_guards()` dispatches to each new guard. All GARD-07 requirements are covered here: guards are called, and the never-raise property is inherited from the `try/except` wrappers already in each guard.

Output: Two new call lines in `run_guards()`; two new `@pytest.mark.unit` tests appended to `tests/unit/test_guards.py`; full unit suite green.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-guard-expansion/13-CONTEXT.md
@.planning/phases/13-guard-expansion/13-PATTERNS.md
@.planning/phases/13-guard-expansion/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire both new guards into run_guards() (per GARD-07)</name>
  <files>src/guards.py</files>
  <read_first>
    - src/guards.py — read the full file; locate run_guards() (at approximately line 47); confirm _check_technology_substitution and _check_protected_sections are both defined above it; note the exact parameter names used in the existing call lines (_check_missing_sections(original_text, tailored_text), etc.)
    - .planning/phases/13-guard-expansion/13-PATTERNS.md — §run_guards() integration point (lines 79-88); shows exact call-site placement and parameter names
    - .planning/phases/13-guard-expansion/13-CONTEXT.md — GARD-07: "Both new guards are called from run_guards() and never raise — all exceptions are caught and logged as warnings"
  </read_first>
  <behavior>
    - After the edit, run_guards() body contains exactly five guard calls: _check_missing_sections, _check_format_violations, _check_hallucinated_employers, _check_technology_substitution, _check_protected_sections
    - _check_technology_substitution is called with (original_text, tailored_text) — matching the signature defined in Plan 01
    - _check_protected_sections is called with (original_text, tailored_text) — matching the signature defined in Plan 02
    - Call order within run_guards(): existing three guards first, then _check_technology_substitution, then _check_protected_sections
    - No other lines in src/guards.py are modified
  </behavior>
  <action>
    Add exactly two lines to the body of run_guards() in src/guards.py, after the existing _check_hallucinated_employers(original_text, tailored_text) call:

      _check_technology_substitution(original_text, tailored_text)
      _check_protected_sections(original_text, tailored_text)

    The indentation must match the existing call lines (4-space indent). No new imports are needed. Do not change the function signature, docstring (if any), or any other existing guard call.

    The never-raise property for GARD-07 is already satisfied by the try/except wrappers inside each guard function (D-13, implemented in Plans 01 and 02). No additional exception handling is needed in run_guards() itself.

    After editing, run `grep -n "_check_technology_substitution\|_check_protected_sections" src/guards.py` to confirm both names appear in the file: once in each function definition and once each in run_guards().
  </action>
  <verify>
    <automated>grep -n "_check_technology_substitution\|_check_protected_sections" src/guards.py && pytest -m unit tests/unit/test_guards.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep output contains a line matching "def _check_technology_substitution(" (from Plan 01 implementation)
    - grep output contains a line matching "def _check_protected_sections(" (from Plan 02 implementation)
    - grep output contains a line matching "_check_technology_substitution(original_text, tailored_text)" inside run_guards (no "def" prefix)
    - grep output contains a line matching "_check_protected_sections(original_text, tailored_text)" inside run_guards (no "def" prefix)
    - pytest src/guards_test.py exits 0 (existing guard tests unaffected)
    - pytest -m unit tests/unit/test_guards.py exits 0 (all twelve Plan 01+02 tests unaffected)
  </acceptance_criteria>
  <done>Both new guards are called from run_guards() with the correct arguments; all existing tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GARD-07 integration tests to test_guards.py</name>
  <files>tests/unit/test_guards.py</files>
  <read_first>
    - tests/unit/test_guards.py — read the full file (twelve tests exist after Plans 01 and 02); understand the import line, existing test shapes, and the call_args_list + any() assertion idiom
    - src/guards.py — confirm run_guards() signature: run_guards(original_text, tailored_text, fences_stripped=False); confirm both new guards are now called from it (after Task 1)
    - .planning/phases/13-guard-expansion/13-PATTERNS.md — §patch("guards.logger") mock pattern and §call_args_list + any() assertion pattern
    - src/guards_test.py — lines 14-17 and 46-50 show the call_args_list idiom as used in the existing test suite; also lines 102-107 for the never-raise test shape
  </read_first>
  <behavior>
    - test_run_guards_calls_technology_substitution: calls run_guards() with a Skills-section diff (original has "Python, Java"; tailored has "Python, Go"); patches guards.logger; asserts mock_logger.warning.call_args_list contains a call whose string representation includes "Java" AND "Go" — proving _check_technology_substitution was invoked and issued its warning
    - test_run_guards_new_guards_never_raise: calls run_guards(None, None) with no patch; must complete without raising any exception — proves the never-raise guarantee holds end-to-end through run_guards() for both new guards (the try/except wrappers in each guard catch the AttributeError from None input)
  </behavior>
  <action>
    Append two new test functions to tests/unit/test_guards.py, after the existing twelve tests.

    Update the import line to also import run_guards: change
      from guards import _check_technology_substitution, _check_protected_sections
    to
      from guards import _check_technology_substitution, _check_protected_sections, run_guards

    Test 1 — test_run_guards_calls_technology_substitution:
    Decorate with @pytest.mark.unit. Build original as a raw string containing r"\header{Skills}" + "\nPython, Java\n" + r"\header{Education}" (no fences, no employer macros — minimal valid structure). Build tailored as a raw string with "Python, Go" instead of "Python, Java". Use `with patch("guards.logger") as mock_logger:`, call run_guards(original, tailored), then assert `any("Java" in str(c) for c in mock_logger.warning.call_args_list)` and `any("Go" in str(c) for c in mock_logger.warning.call_args_list)`. This confirms _check_technology_substitution was called and issued its substitution warning (per D-09).

    Test 2 — test_run_guards_new_guards_never_raise:
    Decorate with @pytest.mark.unit. Call run_guards(None, None) with no patch. The test body is a single line — the call itself. No assertion needed: if run_guards raises, the test fails. This covers the GARD-07 requirement that both new guards "never raise — all exceptions are caught and logged as warnings".

    Both tests use @pytest.mark.unit (required by --strict-markers in pyproject.toml).
    No __init__.py in tests/unit/ (established convention from D-11).
    No sys.path.insert needed (pythonpath = ["src"] in pyproject.toml).
  </action>
  <verify>
    <automated>pytest -m unit tests/unit/test_guards.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_guards.py contains exactly fourteen test functions total (twelve from Plans 01+02 plus two new)
    - Import line includes run_guards: "from guards import _check_technology_substitution, _check_protected_sections, run_guards"
    - test_run_guards_calls_technology_substitution is decorated with @pytest.mark.unit
    - test_run_guards_new_guards_never_raise is decorated with @pytest.mark.unit
    - pytest -m unit tests/unit/test_guards.py -v exits 0 with 14 tests collected and all passing
    - pytest src/guards_test.py exits 0 (existing class-based tests unaffected)
    - ruff check src/guards.py exits 0
    - ruff check tests/unit/test_guards.py exits 0
    - grep -c "def _check_technology_substitution" src/guards.py outputs 1
    - grep -c "def _check_protected_sections" src/guards.py outputs 1
    - grep -v "def _check_technology_substitution" src/guards.py | grep -c "_check_technology_substitution(original_text" outputs 1 (exactly one call site in run_guards)
    - grep -v "def _check_protected_sections" src/guards.py | grep -c "_check_protected_sections(original_text" outputs 1 (exactly one call site in run_guards)
  </acceptance_criteria>
  <done>Fourteen unit tests pass; run_guards() dispatches to both new guards; never-raise property is verified end-to-end; all existing tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> run_guards() | Strings from Ollama flow through run_guards() into both new guards; content is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial of Service | run_guards() dispatch to new guards | mitigate | Each new guard has a try/except Exception wrapper (D-13, Plans 01+02); an exception in _check_technology_substitution or _check_protected_sections is caught inside the guard and logged as a warning — run_guards() itself is never interrupted |
| T-13-07 | Tampering | run_guards() call order | accept | Execution order of guards does not affect correctness; each guard is stateless and reads only the input strings; no shared mutable state between guards |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all changes are two call-site additions to src/guards.py and two test functions in tests/unit/test_guards.py |
</threat_model>

<verification>
After completing both tasks:
- `pytest -m unit tests/unit/test_guards.py -v` exits 0 with 14 tests collected and all passing
- `pytest src/guards_test.py` exits 0 (existing class-based guard tests unaffected)
- `ruff check src/guards.py` exits 0 (no lint errors)
- `grep -n "_check_technology_substitution\|_check_protected_sections" src/guards.py` shows exactly four matches: two def lines and two call-site lines in run_guards()
</verification>

<success_criteria>
- `_check_technology_substitution(original_text, tailored_text)` is called from `run_guards()` in src/guards.py (GARD-07)
- `_check_protected_sections(original_text, tailored_text)` is called from `run_guards()` in src/guards.py (GARD-07)
- `test_run_guards_calls_technology_substitution` passes — proving the technology guard fires through run_guards()
- `test_run_guards_new_guards_never_raise` passes — proving run_guards(None, None) does not raise (end-to-end never-raise coverage for GARD-07)
- All fourteen @pytest.mark.unit tests in tests/unit/test_guards.py pass
- All existing tests in src/guards_test.py pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/13-guard-expansion/13-03-SUMMARY.md` when done.
</output>
