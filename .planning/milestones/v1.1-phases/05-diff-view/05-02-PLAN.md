---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/cli.py
  - src/cli_test.py
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03

must_haves:
  truths:
    - "After tailoring, cli.py calls show_diff(resume_text, result.content) before the final print (D-01)"
    - "show_diff() is called outside the try/except block so it cannot prevent the confirmation line from printing"
    - "cli.py imports show_diff from diff_view in alphabetical import order"
    - "All existing cli_test.py tests continue to pass after patching show_diff"
    - "cli_test.py has a test that asserts show_diff is called with the resume text and tailored content on the success path"
  artifacts:
    - path: "src/cli.py"
      provides: "Integrated diff call in main() flow"
      contains: "from diff_view import show_diff"
    - path: "src/cli_test.py"
      provides: "Updated test suite patching show_diff"
      contains: "cli.show_diff"
  key_links:
    - from: "src/cli.py"
      to: "src/diff_view.py"
      via: "from diff_view import show_diff"
      pattern: "from diff_view import show_diff"
    - from: "cli.py main()"
      to: "show_diff(resume_text, result.content)"
      via: "called after write_resume returns, outside try/except"
      pattern: "show_diff.*resume_text.*result.content"
---

<objective>
Wire `show_diff()` into `cli.py` and update `cli_test.py` so all existing tests pass and the new
call site is covered.

Purpose: Activate the diff display in the real CLI workflow. The module exists from Wave 1; this plan
integrates it at the correct position in `main()` per D-01 and ensures the test suite stays green by
patching `cli.show_diff` in tests that go through the success path.

Output:
- `src/cli.py` — `from diff_view import show_diff` import added; `show_diff(resume_text, result.content)`
  call inserted after `write_resume()` returns, outside the try/except block, before the final print
- `src/cli_test.py` — `@patch("cli.show_diff")` added to all success-path tests; one new test asserts
  `show_diff` is called with correct arguments on the happy path
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/PROJECT.md
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/phases/05-diff-view/05-CONTEXT.md
@/workspace/.planning/phases/05-diff-view/05-PATTERNS.md
@/workspace/.planning/phases/05-diff-view/05-01-SUMMARY.md
@/workspace/src/cli.py
@/workspace/src/cli_test.py
@/workspace/src/diff_view.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire show_diff into cli.py</name>
  <files>src/cli.py</files>
  <read_first>
    - src/cli.py — read the full current file before any edits; identify the import block (lines 1-9) and the integration point (lines 48-57: try/except block and final print)
    - .planning/phases/05-diff-view/05-CONTEXT.md — D-01 (exact call order), D-04 (module structure and import)
    - .planning/phases/05-diff-view/05-PATTERNS.md — cli.py section: import block extension pattern (alphabetical order), integration insertion point with exact before/after code
    - .planning/phases/05-diff-view/05-RESEARCH.md — Pattern 4 (cli.py integration patch), Pitfall 3 (show_diff inside try/except kills confirmation)
  </read_first>
  <action>
    Make two changes to src/cli.py:

    1. Import: Add `from diff_view import show_diff` to the imports block. Insert it between
       `from config import BASE_RESUME_PATH, OUTPUT_DIR` and `from guards import run_guards` to maintain
       alphabetical order by module name (d before g). The stdlib imports (`argparse`, `sys`, `pathlib`)
       stay above all local imports unchanged.

    2. Integration call: Insert `show_diff(resume_text, result.content)` on a new line AFTER the
       try/except block closes (after the `sys.exit(1)` line) and BEFORE
       `print(f"Tailored resume written to: {output_path.resolve()}")`. The result is:

       ```
       ...
       except (RuntimeError, ValueError, FileNotFoundError, OSError) as e:
           print(f"Error: {e}", file=sys.stderr)
           sys.exit(1)

       show_diff(resume_text, result.content)
       print(f"Tailored resume written to: {output_path.resolve()}")
       ```

       `show_diff()` must NOT be inside the try/except block — it is non-fatal and must not prevent
       the confirmation print from executing (RESEARCH.md Pitfall 3, CONTEXT.md code_context).

    No other changes. CLAUDE.md rule: no inline comments, no docstrings.
  </action>
  <verify>
    <automated>cd /workspace && grep -n "from diff_view import show_diff" src/cli.py && grep -n "show_diff(resume_text" src/cli.py</automated>
  </verify>
  <done>
    `src/cli.py` contains `from diff_view import show_diff` in the imports block;
    `show_diff(resume_text, result.content)` appears between the except block and the final print;
    `uv run python -c "from cli import main"` succeeds without ImportError.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cli_test.py to patch show_diff and add call-site test</name>
  <files>src/cli_test.py</files>
  <read_first>
    - src/cli_test.py — read the full current file; identify every test function that reaches the success path (i.e., calls both write_resume and generate_tailored_resume successfully), these need @patch("cli.show_diff")
    - src/cli.py — confirm the post-edit state: show_diff is imported from diff_view, called outside try/except; understand the variable names in scope (resume_text, result.content)
    - .planning/phases/05-diff-view/05-PATTERNS.md — captured-print pattern from cli_test.py lines 114-115 (side_effect lambda)
  </read_first>
  <action>
    Update src/cli_test.py with two changes:

    1. Patch `cli.show_diff` in all success-path tests: Any test that exercises the success path
       (where `write_resume` is reached and returns without raising) must add `@patch("cli.show_diff")`
       to its decorator stack. This prevents the real `show_diff` from calling `sys.stdout.isatty()`
       during tests, which could fail or produce unexpected output. The mock parameter must be
       accepted in the function signature (add `mock_show_diff` as the first positional param after
       the decorator stack, following pytest's bottom-up decorator-to-param mapping rule).

       Success-path tests that require this patch: `test_end_sentinel_breaks_loop`,
       `test_eof_treated_as_submission`, `test_progress_message_printed`,
       `test_success_prints_absolute_path`.

       Error-path tests (test_empty_jd_exits_1, test_runtime_error_from_llm_exits_1,
       test_value_error_from_llm_exits_1) do NOT need the patch because they never reach the
       `show_diff` call site.

    2. Add one new test `test_show_diff_called_with_resume_and_tailored`: Patches the same stack as
       `test_end_sentinel_breaks_loop` (mock_guards, mock_input, mock_read, mock_generate,
       mock_write, plus mock_show_diff). Configures mocks identically to the success path.
       Asserts `mock_show_diff.assert_called_once_with("resume text",
       "\\documentclass{article}\n\\end{document}")` — verifying the correct arguments are passed.

    The patch target is `"cli.show_diff"` (not `"diff_view.show_diff"`) because the function is
    looked up from the `cli` module's namespace at call time.

    CLAUDE.md rule: no inline comments, no docstrings.
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest src/cli_test.py -x -q 2>&1</automated>
  </verify>
  <done>
    `uv run pytest src/cli_test.py -x -q` exits 0 with all existing tests plus the new
    `test_show_diff_called_with_resume_and_tailored` test passing;
    `grep -c "cli.show_diff" src/cli_test.py` outputs at least 5 (4 existing success-path tests
    + 1 new test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli.py main() → show_diff() | In-process call with already-validated strings; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | cli.py control flow | accept | show_diff() placed outside try/except; a bug in diff rendering cannot suppress the resume output confirmation or the written file |
| T-05-04 | Denial of Service | show_diff() hanging | accept | difflib is a pure Python stdlib computation; no I/O, no blocking calls; cannot hang |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new external packages installed this phase |
</threat_model>

<verification>
After both tasks complete, run the full suite:

```bash
cd /workspace && uv run pytest src/ tests/ -x -q
```

Expected: all tests pass, exit 0.

Verify integration call site position (outside try/except, before final print):

```bash
cd /workspace && grep -n "show_diff\|Tailored resume written\|sys.exit" src/cli.py
```

Expected output order: `sys.exit(1)` line number < `show_diff(` line number < `Tailored resume written to:` line number.

Verify import alphabetical position:

```bash
cd /workspace && grep -n "^from" src/cli.py
```

Expected: `from config import` before `from diff_view import` before `from guards import`.
</verification>

<success_criteria>
1. `src/cli.py` contains `from diff_view import show_diff` in alphabetical import order
2. `show_diff(resume_text, result.content)` is called outside the try/except block, before the final print
3. `uv run pytest src/cli_test.py -x -q` exits 0 — all tests pass including `test_show_diff_called_with_resume_and_tailored`
4. `uv run pytest src/ tests/ -x -q` exits 0 — full suite green
5. `grep -c "cli.show_diff" src/cli_test.py` outputs at least 5
</success_criteria>

<output>
Create `.planning/phases/05-diff-view/05-02-SUMMARY.md` when done
</output>
