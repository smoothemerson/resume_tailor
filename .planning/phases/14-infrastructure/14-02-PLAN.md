---
phase: "14-infrastructure"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jd_analyzer_test.py
  - src/resume_reader_test.py
  - src/resume_writer_test.py
autonomous: true
requirements:
  - TEST-14
  - TEST-15
  - TEST-16
must_haves:
  truths:
    - "All 11 new unit tests tagged @pytest.mark.unit pass with pytest -m unit"
    - "jd_analyzer_test.py covers all 6 _parse_analysis_response cases"
    - "resume_reader_test.py covers read_resume existing-file and missing-file cases"
    - "resume_writer_test.py covers write_resume directory creation, timestamp filename, and content fidelity"
  artifacts:
    - path: "src/jd_analyzer_test.py"
      provides: "6 unit tests for _parse_analysis_response"
      contains: "@pytest.mark.unit"
    - path: "src/resume_reader_test.py"
      provides: "2 unit tests for read_resume"
      contains: "@pytest.mark.unit"
    - path: "src/resume_writer_test.py"
      provides: "4 unit tests for write_resume"
      contains: "@pytest.mark.unit"
  key_links:
    - from: "src/jd_analyzer_test.py"
      to: "src/jd_analyzer.py"
      via: "from jd_analyzer import _parse_analysis_response"
      pattern: "_parse_analysis_response"
    - from: "src/resume_reader_test.py"
      to: "src/resume_reader.py"
      via: "from resume_reader import read_resume"
      pattern: "read_resume"
    - from: "src/resume_writer_test.py"
      to: "src/resume_writer.py"
      via: "from resume_writer import write_resume"
      pattern: "write_resume"
---

<objective>
Create three src-colocated test files covering _parse_analysis_response, read_resume, and write_resume — filling the unit test gaps specified by TEST-14, TEST-15, and TEST-16.

Purpose: These functions had no src/-colocated tests. The new files follow the established *_test.py naming convention and @pytest.mark.unit decorator pattern.

Output: src/jd_analyzer_test.py (6 tests), src/resume_reader_test.py (2 tests), src/resume_writer_test.py (4 tests) — 12 total tests all tagged @pytest.mark.unit and passing with pytest -m unit.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-infrastructure/14-CONTEXT.md
@.planning/phases/14-infrastructure/14-RESEARCH.md
@.planning/phases/14-infrastructure/14-PATTERNS.md
@src/jd_analyzer.py
@src/resume_reader.py
@src/resume_writer.py
@src/llm_client_test.py
@tests/unit/test_resume_reader.py
@tests/unit/test_resume_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/jd_analyzer_test.py — 6 tests for _parse_analysis_response (TEST-14, D-01, D-02, D-04)</name>
  <files>src/jd_analyzer_test.py</files>
  <read_first>
    - src/jd_analyzer.py — read the full _parse_analysis_response function to understand its logic (fence stripping, json.loads, dict validation, key presence check, list-type check) before writing any test case; each guard in the function maps to a test case
    - src/llm_client_test.py — read for the *_test.py naming convention and pytest-function style; note it does NOT use @pytest.mark.unit (see PATTERNS.md note — new files must use the marker)
    - tests/unit/test_jd_analyzer.py — read for @pytest.mark.unit decorator placement and import style; this file tests analyze_job_description() with mocks, NOT _parse_analysis_response() directly
    - .planning/phases/14-infrastructure/14-PATTERNS.md — jd_analyzer_test.py section has the full 6-case table and exact test code
  </read_first>
  <behavior>
    - test_parse_valid_json_returns_dict: input '{"technologies": ["Python"], "requirements": ["5 yrs"], "emphasis_areas": ["ML"]}' -> result is dict, set(result.keys()) == {"technologies", "requirements", "emphasis_areas"}
    - test_parse_missing_key_returns_none: input '{"technologies": ["Python"], "requirements": ["5 yrs"]}' (no emphasis_areas) -> None
    - test_parse_non_list_value_returns_none: input '{"technologies": "Python", "requirements": ["5 yrs"], "emphasis_areas": ["ML"]}' (technologies is str not list) -> None
    - test_parse_fenced_json_returns_dict: input '```json\n{"technologies": ["Go"], "requirements": ["3 yrs"], "emphasis_areas": ["backend"]}\n```' -> result is not None (fence stripping works)
    - test_parse_non_json_string_returns_none: input "not json at all" -> None
    - test_parse_empty_string_returns_none: input "" -> None
  </behavior>
  <action>
    Create src/jd_analyzer_test.py. Import pattern (per PATTERNS.md): import pytest, then from jd_analyzer import _parse_analysis_response. No other imports needed — the function is pure (no HTTP, no side effects). Use bare module import (not from src.jd_analyzer) because pyproject.toml sets pythonpath = ["src"]. Write 6 test functions, each decorated with @pytest.mark.unit. Use plain def test_*() style — NOT unittest.TestCase (guards_test.py is the legacy pattern; new files follow the pytest-function style per PATTERNS.md shared patterns). Per D-02: _parse_analysis_response is tested directly with string inputs only. No mocking. Per D-04: every test function must have @pytest.mark.unit.
  </action>
  <verify>
    <automated>cd /workspace/.claude/worktrees/phase14 && uv run pytest src/jd_analyzer_test.py -m unit -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/jd_analyzer_test.py exists
    - File contains exactly 6 test functions (grep -c "^def test_" src/jd_analyzer_test.py returns 6)
    - All 6 functions decorated with @pytest.mark.unit (grep -c "@pytest.mark.unit" src/jd_analyzer_test.py returns 6)
    - Import line is "from jd_analyzer import _parse_analysis_response" (not from src.jd_analyzer)
    - uv run pytest src/jd_analyzer_test.py -m unit exits 0 with "6 passed"
    - No unittest.TestCase usage in the file
  </acceptance_criteria>
  <done>pytest src/jd_analyzer_test.py -m unit reports 6 passed, 0 failed, 0 errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/resume_reader_test.py and src/resume_writer_test.py (TEST-15, TEST-16, D-01, D-03, D-04)</name>
  <files>src/resume_reader_test.py, src/resume_writer_test.py</files>
  <read_first>
    - src/resume_reader.py — read the full file to understand read_resume signature and behavior (4 lines; reads Path, returns str, raises FileNotFoundError on missing file)
    - src/resume_writer.py — read the full file to understand write_resume signature (str, Path -> Path), directory creation, and timestamp filename format (tailored_resume_YYYYMMDD_HHMMSS.tex)
    - tests/unit/test_resume_reader.py — read for exact test structure; src/resume_reader_test.py is identical (D-03: acceptable duplication)
    - tests/unit/test_resume_writer.py — read for exact test structure; src/resume_writer_test.py copies all 4 tests (PATTERNS.md note: copy all 4, not just the 3 mentioned in TEST-16 spec)
    - .planning/phases/14-infrastructure/14-PATTERNS.md — resume_reader_test.py and resume_writer_test.py sections contain the verbatim code to copy
  </read_first>
  <behavior>
    resume_reader_test.py:
    - test_read_resume_returns_file_content: write "\\documentclass{article}" to tmp_path/"resume.tex", read_resume(that path) == "\\documentclass{article}"
    - test_read_resume_missing_file_raises_file_not_found_error: read_resume(tmp_path/"nonexistent.tex") raises FileNotFoundError

    resume_writer_test.py:
    - test_write_resume_creates_output_directory: output_dir = tmp_path/"new_output"; assert not exists before; write_resume("content", output_dir); assert exists after
    - test_write_resume_returns_path: write_resume("content", tmp_path/"out") returns a Path instance
    - test_write_resume_filename_matches_timestamp_pattern: result.name matches re.match(r"tailored_resume_\d{8}_\d{6}\.tex", result.name)
    - test_write_resume_content_equals_input_string: write_resume(content, tmp_path/"out3"), result.read_text(encoding="utf-8") == content
  </behavior>
  <action>
    Create src/resume_reader_test.py: imports are "import pytest" and "from resume_reader import read_resume". Two test functions, both decorated @pytest.mark.unit, both accepting tmp_path fixture argument. Use encoding="utf-8" in write_text call. Use pytest.raises(FileNotFoundError) for missing file case.

    Create src/resume_writer_test.py: imports are "import re", "from pathlib import Path", "import pytest", "from resume_writer import write_resume". Four test functions, all @pytest.mark.unit, all accepting tmp_path. Use re.match(r"tailored_resume_\d{8}_\d{6}\.tex", result.name) for timestamp check. Use encoding="utf-8" in read_text call. Per D-03: these files are identical copies of tests/unit/test_resume_reader.py and tests/unit/test_resume_writer.py respectively. Per D-04: @pytest.mark.unit on every test function.
  </action>
  <verify>
    <automated>cd /workspace/.claude/worktrees/phase14 && uv run pytest src/resume_reader_test.py src/resume_writer_test.py -m unit -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/resume_reader_test.py exists with exactly 2 test functions (grep -c "^def test_" src/resume_reader_test.py returns 2)
    - src/resume_writer_test.py exists with exactly 4 test functions (grep -c "^def test_" src/resume_writer_test.py returns 4)
    - All 6 functions decorated with @pytest.mark.unit
    - Imports use bare module names: "from resume_reader import read_resume", "from resume_writer import write_resume"
    - uv run pytest src/resume_reader_test.py src/resume_writer_test.py -m unit exits 0 with "6 passed"
    - No unittest.TestCase in either file
    - resume_writer_test.py contains "import re" and "from pathlib import Path"
  </acceptance_criteria>
  <done>pytest src/resume_reader_test.py src/resume_writer_test.py -m unit reports 6 passed, 0 failed, 0 errors</done>
</task>

<task type="auto">
  <name>Task 3: Verify full unit suite still passes</name>
  <files></files>
  <read_first>
    - pyproject.toml — confirm testpaths = ["src", "tests"] so all test files are discovered; confirm addopts = "--strict-markers -ra --import-mode=importlib"
  </read_first>
  <action>
    Run the full pytest -m unit suite to confirm the 3 new src/ test files coexist cleanly with the existing tests in tests/unit/ and src/. No file modifications in this task — verification only. If any test fails, diagnose and fix before marking task done. Common failure modes: missing @pytest.mark.unit decorator, import path using "src.module" instead of "module", test file named with test_ prefix instead of _test.py suffix (though both are discovered under importlib mode).
  </action>
  <verify>
    <automated>cd /workspace/.claude/worktrees/phase14 && uv run pytest -m unit -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest -m unit exits 0
    - Output contains "passed" with no "failed" or "error"
    - The 12 new tests (6 + 2 + 4) all appear in the collected and passed output
    - No PytestUnknownMarkWarning or marker-related warnings
  </acceptance_criteria>
  <done>Full pytest -m unit suite exits 0 with all existing tests plus 12 new tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src/ modules | Tests import and call production functions directly |
| tmp_path → filesystem | pytest creates isolated temp directories; no cross-test contamination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | src/*_test.py | accept | Test files contain no secrets or credentials; worst case is incorrect test assertions which are caught by CI |
| T-14-05 | Repudiation | pytest -m unit | accept | Tests are tagged @pytest.mark.unit and --strict-markers is set; unmarked tests cannot silently bypass the marker filter |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero new packages; all test infrastructure (pytest, tmp_path) already in dev dependencies |
</threat_model>

<verification>
After all 3 tasks complete:
1. `grep -c "^def test_" src/jd_analyzer_test.py` returns 6
2. `grep -c "^def test_" src/resume_reader_test.py` returns 2
3. `grep -c "^def test_" src/resume_writer_test.py` returns 4
4. `uv run pytest src/jd_analyzer_test.py src/resume_reader_test.py src/resume_writer_test.py -m unit` exits 0 with 12 passed
5. `uv run pytest -m unit` exits 0 (all existing + new tests)
</verification>

<success_criteria>
- src/jd_analyzer_test.py: 6 tests, all @pytest.mark.unit, all passing — covers valid JSON, missing key, non-list value, fenced JSON, non-JSON string, empty string
- src/resume_reader_test.py: 2 tests, all @pytest.mark.unit, all passing — covers existing file returns content, missing file raises FileNotFoundError
- src/resume_writer_test.py: 4 tests, all @pytest.mark.unit, all passing — covers directory creation, returns Path, timestamp filename pattern, content fidelity
- Full pytest -m unit suite exits 0
</success_criteria>

<output>
Create `.planning/phases/14-infrastructure/14-02-SUMMARY.md` when done
</output>
