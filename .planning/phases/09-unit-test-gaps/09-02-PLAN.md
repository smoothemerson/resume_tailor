---
phase: 09-unit-test-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_resume_reader.py
  - tests/unit/test_resume_writer.py
autonomous: true
requirements:
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "pytest -m unit tests/unit/test_resume_reader.py passes with zero Ollama dependency"
    - "pytest -m unit tests/unit/test_resume_writer.py passes with zero Ollama dependency"
    - "read_resume() returns file text when the file exists"
    - "read_resume() raises FileNotFoundError when the file does not exist"
    - "write_resume() creates the output directory when it does not exist"
    - "write_resume() returns a pathlib.Path"
    - "write_resume() written filename matches tailored_resume_YYYYMMDD_HHMMSS.tex pattern"
    - "write_resume() file content on disk equals the input string"
  artifacts:
    - path: "tests/unit/test_resume_reader.py"
      provides: "Unit tests for read_resume"
      contains: "@pytest.mark.unit"
    - path: "tests/unit/test_resume_writer.py"
      provides: "Unit tests for write_resume"
      contains: "@pytest.mark.unit"
  key_links:
    - from: "tests/unit/test_resume_reader.py"
      to: "src/resume_reader.py"
      via: "from resume_reader import read_resume"
      pattern: "from resume_reader import read_resume"
    - from: "tests/unit/test_resume_writer.py"
      to: "src/resume_writer.py"
      via: "from resume_writer import write_resume"
      pattern: "from resume_writer import write_resume"
---

<objective>
Create tests/unit/test_resume_reader.py (TEST-06, per D-02) and tests/unit/test_resume_writer.py (TEST-07, per D-02, D-05, D-06, D-07) using pytest's tmp_path fixture with @pytest.mark.unit on every test (D-03). Both files are purely file-I/O tests with no mocking or network calls.

Purpose: Close TEST-06 and TEST-07 unit test gaps identified in ROADMAP Phase 9 goal.
Output: Two test files totalling 6 test functions, all marked @pytest.mark.unit, all passing under pytest -m unit.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unit-test-gaps/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/unit/test_resume_reader.py</name>
  <files>tests/unit/test_resume_reader.py</files>
  <read_first>
    - src/resume_reader.py — read the full file to verify the function signature (read_resume(path: Path) -> str), the try/except block that re-raises FileNotFoundError with a new instance using "raise FileNotFoundError(...) from exc" (affects test assertion strategy — use pytest.raises(FileNotFoundError) only, do not inspect the exception message or cause chain per RESEARCH.md Pitfall 3)
    - pyproject.toml — confirm pythonpath = ["src"] so "from resume_reader import read_resume" resolves without sys.path hacks
  </read_first>
  <behavior>
    Two test functions for read_resume (TEST-06, per D-02):
    - test_read_resume_returns_file_content: create tmp_path / "resume.tex" with write_text("\\documentclass{article}", encoding="utf-8"), call read_resume(resume_file), assert result == "\\documentclass{article}"
    - test_read_resume_missing_file_raises_file_not_found_error: call read_resume(tmp_path / "nonexistent.tex") with pytest.raises(FileNotFoundError) — do NOT inspect excinfo.value.__cause__ or assert on exception message text
  </behavior>
  <action>
    Create tests/unit/test_resume_reader.py with imports: pytest and from resume_reader import read_resume (per D-02). Do NOT import pathlib.Path — tmp_path yields a Path directly (per RESEARCH.md Pattern 5).

    Apply @pytest.mark.unit to both test functions (per D-03).

    Happy-path test: use tmp_path fixture, create tmp_path / "resume.tex" using write_text with encoding="utf-8", assert read_resume returns the exact string written.

    Error-path test: pass a path to a file that does not exist (tmp_path / "nonexistent.tex" — do not call write_text for this path). Assert pytest.raises(FileNotFoundError). Keep the assertion minimal — the source re-raises a new FileNotFoundError instance with a custom message; asserting on the message text would be brittle.

    No mocking needed — read_resume is a thin wrapper around pathlib.Path.read_text and tmp_path provides a real filesystem.
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest -m unit tests/unit/test_resume_reader.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_resume_reader.py exists
    - File contains exactly 2 test functions
    - Both test functions have @pytest.mark.unit decorator
    - uv run pytest -m unit tests/unit/test_resume_reader.py exits 0 with 2 passed
    - Happy-path test creates a real file in tmp_path and asserts return value equals the written content
    - Error-path test uses pytest.raises(FileNotFoundError) without inspecting message or cause
    - No sys.path manipulation, no conftest imports, no Ollama fixtures
  </acceptance_criteria>
  <done>2 unit tests pass; TEST-06 requirement satisfied</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/unit/test_resume_writer.py</name>
  <files>tests/unit/test_resume_writer.py</files>
  <read_first>
    - src/resume_writer.py — read the full file to verify: write_resume(content: str, output_dir: Path) -> Path, output_dir.mkdir(parents=True, exist_ok=True) is the first line, datetime.now().strftime("%Y%m%d_%H%M%S") produces the 8-digit date + 6-digit time in the filename, and the function returns the written Path
    - pyproject.toml — confirm pythonpath = ["src"] resolves "from resume_writer import write_resume"
  </read_first>
  <behavior>
    Four test functions for write_resume (TEST-07, per D-02, D-05, D-06, D-07):
    - test_write_resume_creates_output_directory: output_dir = tmp_path / "new_output", assert not output_dir.exists(), call write_resume("content", output_dir), assert output_dir.exists(). Must use a sub-path not tmp_path directly (per RESEARCH.md Pitfall 4 — passing tmp_path directly means mkdir is a no-op and the directory-creation behavior is never exercised)
    - test_write_resume_returns_path: result = write_resume("content", tmp_path / "out"), assert isinstance(result, Path)
    - test_write_resume_filename_matches_timestamp_pattern: result = write_resume("content", tmp_path / "out2"), assert re.match(r"tailored_resume_\d{8}_\d{6}\.tex", result.name). Use re.match, not datetime mocking (per D-05)
    - test_write_resume_content_equals_input_string: content = "\\documentclass{article}\n\\end{document}", result = write_resume(content, tmp_path / "out3"), assert result.read_text(encoding="utf-8") == content (per D-06)
  </behavior>
  <action>
    Create tests/unit/test_resume_writer.py with imports: re, pytest, pathlib.Path (needed for isinstance assertion), and from resume_writer import write_resume (per D-02). All imports at top.

    Apply @pytest.mark.unit to all four test functions (per D-03).

    Directory-creation test: use tmp_path / "new_output" as output_dir (a subdirectory that does not yet exist). Assert it does not exist before the call, then assert it exists after the call. Do NOT pass tmp_path directly.

    Returns-Path test and filename-pattern test: each uses a distinct sub-path (tmp_path / "out", tmp_path / "out2") to avoid any cross-test state. The filename pattern regex is r"tailored_resume_\d{8}_\d{6}\.tex" exactly (per D-05) — no datetime mocking, no strptime.

    Content-roundtrip test: write a multi-line LaTeX string, read back with encoding="utf-8", assert equality (per D-06). Use tmp_path / "out3" or any fresh sub-path.

    No mocking needed — write_resume only uses datetime.now() and pathlib operations; tmp_path provides the filesystem.
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest -m unit tests/unit/test_resume_writer.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_resume_writer.py exists
    - File contains exactly 4 test functions
    - All 4 test functions have @pytest.mark.unit decorator
    - uv run pytest -m unit tests/unit/test_resume_writer.py exits 0 with 4 passed
    - Directory-creation test uses tmp_path / "new_output" (not tmp_path directly) and asserts both before and after states
    - Filename pattern test uses re.match(r"tailored_resume_\d{8}_\d{6}\.tex", result.name) — no datetime mocking
    - Content-roundtrip test reads the written file with encoding="utf-8" and asserts == input string
    - Returns-Path test uses isinstance(result, Path)
    - No sys.path manipulation, no conftest imports, no Ollama fixtures
  </acceptance_criteria>
  <done>4 unit tests pass; TEST-07 requirement satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → filesystem | Tests write and read files via tmp_path — all confined to pytest's temp directory, auto-cleaned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | tests/unit/test_resume_reader.py | accept | Test-only file; reads from tmp_path only; no production path |
| T-09-03 | Tampering | tests/unit/test_resume_writer.py | accept | Test-only file; writes to tmp_path only; no production path |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already present in pyproject.toml |
</threat_model>

<verification>
uv run pytest -m unit tests/unit/test_resume_reader.py -v
uv run pytest -m unit tests/unit/test_resume_writer.py -v
uv run pytest -m unit tests/unit/ -v
</verification>

<success_criteria>
- pytest -m unit tests/unit/test_resume_reader.py passes (2 tests)
- pytest -m unit tests/unit/test_resume_writer.py passes (4 tests)
- pytest -m unit tests/unit/ passes all 6 tests (from this plan) plus all tests from 09-01 (12 tests) = 18 total, under 2 seconds
- TEST-06 covered: returns content when file exists; raises FileNotFoundError when file absent
- TEST-07 covered: directory created when absent; returns Path; filename matches timestamp pattern; content roundtrips
- D-05 honored: filename assertion uses re.match, no datetime mocking
- D-06 honored: content roundtrip assertion in write_resume test
- D-07 honored: tmp_path used as base for all output_dir arguments
</success_criteria>

<output>
Create .planning/phases/09-unit-test-gaps/09-02-SUMMARY.md when done
</output>
