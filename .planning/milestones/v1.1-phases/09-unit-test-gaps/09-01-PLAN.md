---
phase: 09-unit-test-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_llm_client.py
autonomous: true
requirements:
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "pytest -m unit tests/unit/test_llm_client.py passes in under 2 seconds with zero Ollama dependency"
    - "_build_messages() returns a 2-element list with role 'system' first, then 'user'"
    - "_build_messages() system message contains <PERSONA> and <CONSTRAINTS> tags"
    - "_build_messages() user message contains <job_description> and <resume> tags embedding the provided strings"
    - "_check_ollama_health() raises RuntimeError on ConnectionError (per TEST-05)"
    - "_check_ollama_health() raises RuntimeError on Timeout (per TEST-05)"
    - "_check_ollama_health() raises RuntimeError on HTTPError via raise_for_status (per D-10)"
    - "_check_ollama_health() does not raise when requests.get returns HTTP 200 (per TEST-05)"
  artifacts:
    - path: "tests/unit/test_llm_client.py"
      provides: "Unit tests for _build_messages and _check_ollama_health"
      contains: "@pytest.mark.unit"
  key_links:
    - from: "tests/unit/test_llm_client.py"
      to: "src/llm_client.py"
      via: "from llm_client import _build_messages, _check_ollama_health"
      pattern: "from llm_client import _build_messages, _check_ollama_health"
    - from: "tests/unit/test_llm_client.py"
      to: "llm_client.requests.get"
      via: "@patch(\"llm_client.requests.get\")"
      pattern: "patch.*llm_client\\.requests\\.get"
---

<objective>
Create tests/unit/test_llm_client.py covering _build_messages() (TEST-04, D-02) and _check_ollama_health() (TEST-05, D-08, D-09, D-10) with @pytest.mark.unit on every test function (D-03). All tests are isolated — no Ollama dependency.

Purpose: Close TEST-04 and TEST-05 unit test gaps identified in ROADMAP Phase 9 goal.
Output: tests/unit/test_llm_client.py with 12 test functions, all marked @pytest.mark.unit, all passing under pytest -m unit.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unit-test-gaps/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/unit/test_llm_client.py with _build_messages and _check_ollama_health tests</name>
  <files>tests/unit/test_llm_client.py</files>
  <read_first>
    - src/llm_client.py — read the full file to verify _build_messages signature (resume_text: str, job_description: str) -> list[dict], the exact XML tags in the prompts (<PERSONA>, <CONSTRAINTS>, <job_description>, <resume>), and the _check_ollama_health exception branches (ConnectionError, Timeout, HTTPError via raise_for_status)
    - src/llm_client_test.py — read for the established patch target pattern (@patch("llm_client.requests.get")), MagicMock(status_code=200) pattern, and import ordering conventions
    - pyproject.toml — confirm markers declaration and addopts to validate @pytest.mark.unit is a registered marker
  </read_first>
  <behavior>
    _build_messages tests (TEST-04, per D-02):
    - test_build_messages_returns_two_element_list: _build_messages("resume text", "job description") returns list of len 2
    - test_build_messages_role_ordering: result[0]["role"] == "system" and result[1]["role"] == "user"
    - test_build_messages_system_contains_persona_tag: "<PERSONA>" in result[0]["content"]
    - test_build_messages_system_contains_constraints_tag: "<CONSTRAINTS>" in result[0]["content"]
    - test_build_messages_user_contains_job_description_xml_tag: "<job_description>" in result[1]["content"]
    - test_build_messages_user_embeds_job_description_content: "python developer" in result[1]["content"] when called with job_description="python developer"
    - test_build_messages_user_contains_resume_xml_tag: "<resume>" in result[1]["content"]
    - test_build_messages_user_embeds_resume_content: "my specific resume" in result[1]["content"] when called with resume_text="my specific resume"

    _check_ollama_health tests (TEST-05 + D-10, per D-08, D-09):
    - test_check_ollama_health_connection_error_raises_runtime_error: @patch("llm_client.requests.get") with side_effect=requests.ConnectionError("connection refused") → pytest.raises(RuntimeError)
    - test_check_ollama_health_timeout_raises_runtime_error: @patch("llm_client.requests.get") with side_effect=requests.Timeout("timed out") → pytest.raises(RuntimeError)
    - test_check_ollama_health_http_error_raises_runtime_error: @patch("llm_client.requests.get"), mock_response = MagicMock(), mock_response.raise_for_status.side_effect = requests.HTTPError("503"), mock_get.return_value = mock_response → pytest.raises(RuntimeError). Do NOT set side_effect on mock_get directly (see RESEARCH.md Pitfall 1 and Pattern 3)
    - test_check_ollama_health_200_does_not_raise: @patch("llm_client.requests.get") with return_value=MagicMock(status_code=200) → function returns None without raising
  </behavior>
  <action>
    Create tests/unit/test_llm_client.py with imports: pytest, requests, unittest.mock.MagicMock, unittest.mock.patch, and from llm_client import _build_messages, _check_ollama_health (per D-02).

    Apply @pytest.mark.unit to every test function — no exceptions (per D-03). Do NOT add markers to any file in src/ (per D-04).

    For _build_messages tests: call the function directly with string arguments, assert on list structure and string membership in message content. Do NOT assert on exact prompt prose — assert structural markers and XML tags only (per ROADMAP success criteria item 2).

    For _check_ollama_health ConnectionError and Timeout paths: set side_effect directly on mock_get — these exceptions are raised by requests.get() itself (per RESEARCH.md Pattern 1).

    For _check_ollama_health HTTPError path: set side_effect on mock_response.raise_for_status, NOT on mock_get. HTTPError is raised by response.raise_for_status() in the source — setting it on mock_get causes the wrong code path to be exercised (per RESEARCH.md Pitfall 1 and Pattern 3).

    For the HTTP 200 success path: use mock_get.return_value = MagicMock(status_code=200). MagicMock auto-stubs raise_for_status() as a no-op. Call _check_ollama_health() and assert the return value is None (per RESEARCH.md open question resolution — documents the implicit None contract).

    No conftest.py Ollama fixtures (require_ollama) in any of these tests — zero network dependency required (per RESEARCH.md anti-patterns).
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest -m unit tests/unit/test_llm_client.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_llm_client.py exists
    - File contains exactly 12 test functions (8 for _build_messages, 4 for _check_ollama_health)
    - Every test function has @pytest.mark.unit decorator
    - uv run pytest -m unit tests/unit/test_llm_client.py exits 0 with 12 passed
    - uv run pytest -m unit tests/unit/test_llm_client.py completes in under 2 seconds
    - uv run pytest -m unit tests/unit/ --co shows test_llm_client.py tests collected with "unit" marker
    - No test uses the require_ollama fixture or makes real network calls
    - The HTTPError test sets side_effect on mock_response.raise_for_status (not on mock_get directly)
    - @patch target is "llm_client.requests.get" (not "requests.get")
  </acceptance_criteria>
  <done>12 unit tests pass under pytest -m unit with zero Ollama dependency; TEST-04 and TEST-05 requirements satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → source code | Unit tests import and call internal functions directly — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | tests/unit/test_llm_client.py | accept | Test-only file; no production code path; no security surface |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already present in pyproject.toml |
</threat_model>

<verification>
uv run pytest -m unit tests/unit/test_llm_client.py -v
uv run pytest -m unit tests/unit/ --co
</verification>

<success_criteria>
- pytest -m unit tests/unit/test_llm_client.py passes (12 tests, under 2 seconds)
- All 12 tests carry @pytest.mark.unit and appear in --collect-only output for -m unit
- TEST-04 covered: 2-element list, role ordering, system contains <PERSONA> and <CONSTRAINTS>, user contains <job_description> and <resume> with embedded content
- TEST-05 covered: ConnectionError → RuntimeError, Timeout → RuntimeError, HTTPError → RuntimeError, HTTP 200 → no raise
- D-10 covered: HTTPError path tested via raise_for_status side_effect (4th path for _check_ollama_health)
</success_criteria>

<output>
Create .planning/phases/09-unit-test-gaps/09-01-SUMMARY.md when done
</output>
