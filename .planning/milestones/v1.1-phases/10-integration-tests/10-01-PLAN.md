---
phase: "10-integration-tests"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_llm_client.py
autonomous: true
requirements:
  - TEST-08
  - TEST-09

must_haves:
  truths:
    - "pytest -m integration with Ollama running exits 0 with 2 PASSED"
    - "pytest -m integration with Ollama stopped exits 0 with 2 SKIPPED and reason 'Ollama not available'"
    - "Integration tests use a minimal inline fixture resume, not english.tex or any external file"
  artifacts:
    - path: "tests/integration/test_llm_client.py"
      provides: "TEST-08 and TEST-09 integration test functions"
      contains: "MINIMAL_RESUME"
      exports: ["test_ollama_health_check_does_not_raise", "test_generate_tailored_resume_returns_valid_latex"]
  key_links:
    - from: "tests/integration/test_llm_client.py"
      to: "tests/conftest.py"
      via: "require_ollama fixture parameter injection"
      pattern: "def test_.*require_ollama"
    - from: "tests/integration/test_llm_client.py"
      to: "src/llm_client.py"
      via: "direct import"
      pattern: "from llm_client import _check_ollama_health, generate_tailored_resume"
---

<objective>
Create tests/integration/test_llm_client.py with two integration tests that make real Ollama calls.
TEST-08 verifies _check_ollama_health() does not raise when Ollama is running (per D-03).
TEST-09 verifies generate_tailored_resume() returns structurally valid LaTeX with four assertions (per D-04).
Both tests skip gracefully when Ollama is unreachable using the existing require_ollama fixture (per D-06).

Purpose: Close the gap between the mocked unit tests (Phase 9) and a live Ollama instance — structural invariants are tested against real model output, not a mock.
Output: tests/integration/test_llm_client.py — 1 new file, no modifications to existing files.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/REQUIREMENTS.md
@/workspace/.planning/phases/10-integration-tests/10-CONTEXT.md
@/workspace/.planning/phases/10-integration-tests/10-RESEARCH.md
@/workspace/.planning/phases/10-integration-tests/10-PATTERNS.md
@/workspace/src/llm_client.py
@/workspace/tests/conftest.py
@/workspace/pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/integration/test_llm_client.py with TEST-08 and TEST-09</name>
  <files>tests/integration/test_llm_client.py</files>

  <read_first>
    - src/llm_client.py — confirms _check_ollama_health() signature (no args, returns None, raises RuntimeError on failure), generate_tailored_resume(resume_text: str, job_description: str, model: str | None = None) -> TailorResult, and TailorResult fields (.content: str, .fences_stripped: bool)
    - tests/conftest.py — confirms require_ollama fixture signature (accepts ollama_available: bool, calls pytest.skip("Ollama not available") when False); ollama_available is session-scoped
    - tests/unit/test_llm_client.py — import convention: "from llm_client import ..." (no package prefix); @pytest.mark.unit on every function; confirms no __init__.py needed
    - pyproject.toml — confirms markers = ["unit:...", "integration:...", "e2e:..."] and addopts = "--strict-markers -ra"; confirms pythonpath = ["src"]
  </read_first>

  <action>
    Create tests/integration/test_llm_client.py. The file has no __init__.py in its directory (bare directory layout established in Phase 8 — do not add one).

    File structure (top to bottom):

    1. Import block: import pytest, then blank line, then "from llm_client import _check_ollama_health, generate_tailored_resume". No unittest.mock imports — no patching in integration tests.

    2. Module-level MINIMAL_RESUME constant (per D-02) — a multi-line string using parenthesized string concatenation:
       - Line 1: "\\documentclass{article}\n"
       - Line 2: "\\begin{document}\n"
       - Line 3: "\\section{Summary}\n"
       - Line 4: "AI engineer with 3 years experience.\n"
       - Line 5: "\\end{document}"
       The constant must be defined at module level, not inside a fixture or test function (per D-02).

    3. TEST-08 function (per D-03, D-05, D-06):
       - Decorated with @pytest.mark.integration
       - Signature: def test_ollama_health_check_does_not_raise(require_ollama):
       - Body: single call to _check_ollama_health() — no assertions needed; not raising IS the assertion
       - require_ollama parameter causes pytest to inject the conftest fixture; if Ollama is down, pytest.skip() fires before the body runs

    4. TEST-09 function (per D-04, D-05, D-06):
       - Decorated with @pytest.mark.integration
       - Signature: def test_generate_tailored_resume_returns_valid_latex(require_ollama):
       - Inline JD constant inside the function body: MINIMAL_JD = "Python backend engineer with REST API experience"
       - Call: result = generate_tailored_resume(MINIMAL_RESUME, MINIMAL_JD)
       - Four assertions from D-04 in this exact order:
         assert result.content.lstrip().startswith("\\documentclass")
         assert result.content.rstrip().endswith("\\end{document}")
         assert "```" not in result.content
         assert result.fences_stripped is False
       - require_ollama parameter triggers skip when Ollama is absent (per D-06)

    Naming follows the raise-not-exit pattern established in Phase 9 — tests call functions directly and let exceptions propagate as test failures (not sys.exit). Do not monkeypatch TIMEOUT. Do not add @pytest.fixture for MINIMAL_RESUME. Do not redefine require_ollama or ollama_available in this file.
  </action>

  <verify>
    <automated>cd /workspace && uv run pytest tests/integration/test_llm_client.py --collect-only -q 2>&1 | grep -E "(test_ollama_health_check_does_not_raise|test_generate_tailored_resume_returns_valid_latex|ERROR|error)"</automated>
  </verify>

  <acceptance_criteria>
    - pytest --collect-only discovers exactly 2 tests in tests/integration/test_llm_client.py: test_ollama_health_check_does_not_raise and test_generate_tailored_resume_returns_valid_latex
    - uv run pytest tests/integration/test_llm_client.py -v exits 0 when Ollama is down, showing 2 SKIPPED with reason "Ollama not available"
    - uv run pytest tests/integration/test_llm_client.py -v exits 0 when Ollama is running, showing 2 PASSED
    - uv run pytest -m "not integration" exits 0 (no regressions in unit tests)
    - uv run pytest --collect-only exits 0 with no warnings (--strict-markers respected; integration marker registered)
    - tests/integration/test_llm_client.py contains "MINIMAL_RESUME" (module-level constant, per D-02)
    - tests/integration/test_llm_client.py contains "@pytest.mark.integration" (both test functions carry the marker, per D-05)
    - tests/integration/test_llm_client.py contains "require_ollama" in both function signatures (per D-06)
    - tests/integration/test_llm_client.py does NOT contain "unittest.mock", "__init__", "sys.path", or "pytest.fixture" (no re-implementation of existing infrastructure)
    - tests/integration/test_llm_client.py contains all four D-04 assertions: lstrip().startswith, rstrip().endswith, "```" not in, fences_stripped is False
  </acceptance_criteria>

  <done>tests/integration/test_llm_client.py exists with 2 integration tests — both skip when Ollama is absent (exit 0) and both pass when Ollama is running with a model loaded; all four D-04 structural assertions are present for TEST-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → localhost:11434 | Test code makes outbound HTTP GET/POST to Ollama on loopback; response is LLM-generated LaTeX string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | test inputs (MINIMAL_RESUME, MINIMAL_JD) | accept | Inputs are hardcoded string constants in the test file; no user-controlled data reaches the test layer; no injection surface |
| T-10-02 | Information Disclosure | LLM response content in test output | accept | LLM output is printed only on test failure; contains no PII; local dev tool with no sensitive data |
| T-10-03 | Denial of Service | 300-second read timeout per TEST-09 | accept | Intentional — TIMEOUT=(10,300) is the production config; overriding it in tests would mask real behavior; single test call, not a loop |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; Package Legitimacy Audit confirmed empty in RESEARCH.md |

**ASVS L1 compliance:** This phase adds test-only code making local HTTP calls to localhost:11434. No new attack surface is introduced on the production code path. V2 (auth), V3 (sessions), V4 (access control), V5 (input validation), and V6 (crypto) do not apply — localhost-only, no auth layer, hardcoded test inputs, plain HTTP. ASVS L1 compliant by design.
</threat_model>

<verification>
1. Test collection: uv run pytest --collect-only discovers both integration tests with no collection errors
2. Skip path (Ollama absent): uv run pytest -m integration exits 0, 2 SKIPPED, reason "Ollama not available"
3. Pass path (Ollama running): uv run pytest -m integration exits 0, 2 PASSED
4. No regression: uv run pytest -m "not integration" exits 0, all unit tests still pass
5. Full suite: uv run pytest exits 0, integration tests skip cleanly if Ollama is down
</verification>

<success_criteria>
1. pytest -m integration with Ollama running exits 0 — response starts with \documentclass, ends with \end{document}, contains no markdown fences (TEST-09 passes)
2. pytest -m integration with Ollama stopped exits 0 — both tests shown as SKIPPED with reason "Ollama not available" (TEST-08 and TEST-09 skip cleanly)
3. Integration tests use MINIMAL_RESUME inline constant — no dependency on english.tex or any external file
</success_criteria>

<output>
Create .planning/phases/10-integration-tests/10-01-SUMMARY.md when done.
</output>
