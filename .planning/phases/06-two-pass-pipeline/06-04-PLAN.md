---
phase: 06-two-pass-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/llm_client.py
autonomous: true
requirements:
  - PIPE-02
  - PIPE-04

must_haves:
  truths:
    - "_build_messages called with analysis=non-None dict returns user message containing <jd_analysis> block with technologies, requirements, emphasis_areas lines"
    - "_build_messages called without analysis (or analysis=None) returns user message with no <jd_analysis> tag"
    - "generate_tailored_resume accepts analysis keyword argument with default None"
    - "All existing llm_client unit tests continue to pass — no breaking changes to existing 2-arg _build_messages calls"
  artifacts:
    - path: "src/llm_client.py"
      provides: "Extended _build_messages and generate_tailored_resume accepting optional analysis dict"
      contains: "analysis: dict | None = None"
  key_links:
    - from: "_build_messages"
      to: "user_message"
      via: "conditional analysis_block prepended when analysis is not None"
      pattern: "jd_analysis"
    - from: "generate_tailored_resume"
      to: "_build_messages"
      via: "passes analysis argument through"
      pattern: "_build_messages.*analysis"
---

<objective>
Extend src/llm_client.py to support the analysis injection path (PIPE-02): add optional analysis parameter to _build_messages and generate_tailored_resume, inject analysis content as a <jd_analysis> XML block in the user message when analysis is not None.

Purpose: Turns the RED test_build_messages_with_analysis_includes_jd_analysis_tag test from Plan 01 GREEN. Enables Plan 05 (cli.py wiring) to pass analysis through to the tailoring call.

Output: src/llm_client.py with extended signatures; all 22 existing unit tests still pass.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/phases/06-two-pass-pipeline/06-CONTEXT.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-RESEARCH.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-PATTERNS.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _build_messages and generate_tailored_resume with optional analysis parameter</name>
  <files>src/llm_client.py</files>
  <read_first>
    - src/llm_client.py — read the entire file to see current signatures (_build_messages at line 26, user_message construction at lines 97-109, generate_tailored_resume signature at lines 132-134, _build_messages call at line 138). Every line number is needed to place edits precisely.
    - .planning/phases/06-two-pass-pipeline/06-PATTERNS.md — llm_client.py modifications section: exact current signature text to replace, exact location of user_message injection block (after the user_message = ... assignment, before the return), exact _build_messages call update
    - .planning/phases/06-two-pass-pipeline/06-RESEARCH.md — _build_messages() Analysis Injection code example showing the analysis_block string format with f-string list interpolation
  </read_first>
  <behavior>
    - test_build_messages_with_analysis_includes_jd_analysis_tag (from plan 01): _build_messages("r", "jd", {"technologies": [], "requirements": [], "emphasis_areas": []}) — result[1]["content"] contains "<jd_analysis>" and "</jd_analysis>"
    - test_build_messages_without_analysis_omits_jd_analysis_tag (from plan 01): _build_messages("r", "jd") — result[1]["content"] does NOT contain "<jd_analysis>"
    - existing test_generate_tailored_resume_truncated_raises and all other generate_tailored_resume tests: still pass (no breaking changes)
    - existing 8 _build_messages tests: still pass (analysis defaults to None, 2-arg calls remain valid)
  </behavior>
  <action>
    Make three targeted edits to src/llm_client.py:

    Edit 1 — _build_messages signature (line 26): Change the function signature from:
      def _build_messages(resume_text: str, job_description: str) -> list[dict]:
    to:
      def _build_messages(resume_text: str, job_description: str, analysis: dict | None = None) -> list[dict]:

    Edit 2 — analysis injection block (after user_message assignment, before the return statement): After the closing parenthesis of the user_message = (...) assignment (currently at the return at line 106), insert a conditional block:
      if analysis is not None:
          analysis_block = (
              "<jd_analysis>\n"
              f"technologies: {analysis['technologies']}\n"
              f"requirements: {analysis['requirements']}\n"
              f"emphasis_areas: {analysis['emphasis_areas']}\n"
              "</jd_analysis>\n\n"
          )
          user_message = analysis_block + user_message
    This block must be placed AFTER the user_message variable is defined and BEFORE the return [...] statement.

    Edit 3 — generate_tailored_resume signature (lines 132-134): Change from:
      def generate_tailored_resume(
          resume_text: str, job_description: str, model: str | None = None
      ) -> TailorResult:
    to:
      def generate_tailored_resume(
          resume_text: str, job_description: str, model: str | None = None, analysis: dict | None = None
      ) -> TailorResult:

    Edit 4 — _build_messages call inside generate_tailored_resume (line 138): Change from:
      messages = _build_messages(resume_text, job_description)
    to:
      messages = _build_messages(resume_text, job_description, analysis)

    No other changes to llm_client.py. The done_reason guard, fence stripping, LaTeX validation, health check, and TailorResult return are untouched.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/unit/test_llm_client.py -m unit -x -q 2>&1</automated>
  </verify>
  <done>
    pytest -m unit tests/unit/test_llm_client.py -x -q exits 0 with all tests passing (22 original + 2 new = 24 total). _build_messages signature contains "analysis: dict | None = None". generate_tailored_resume signature contains "analysis: dict | None = None". _build_messages call inside generate_tailored_resume passes analysis as third argument.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli→llm_client | analysis dict passed from cli.py to generate_tailored_resume — user-controlled via LLM output |
| llm_client→Ollama | analysis content injected into user message of tailoring prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | analysis dict injected into prompt via f-string | accept | Source is the output of analyze_job_description which already validated key presence; content is forwarded to a local LLM with no shell/SQL/template interpolation — no injection surface beyond the LLM context window itself |
| T-06-07 | Information Disclosure | analysis content in tailoring prompt | accept | Localhost only; the job description is user-supplied and intentionally shared with the local LLM |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — modifications to existing file only |
</threat_model>

<verification>
pytest -m unit tests/unit/test_llm_client.py -x -q exits 0
grep "analysis: dict | None = None" src/llm_client.py shows 2 matches (one per function)
grep "jd_analysis" src/llm_client.py shows the injection block
grep "_build_messages(resume_text, job_description, analysis)" src/llm_client.py shows updated call
</verification>

<success_criteria>
src/llm_client.py _build_messages has third parameter analysis: dict | None = None. When analysis is not None, user message is prefixed with <jd_analysis> XML block. generate_tailored_resume has analysis: dict | None = None and passes it to _build_messages. All 24 unit tests in test_llm_client.py pass (22 existing + 2 new from plan 01).
</success_criteria>

<output>
Create .planning/phases/06-two-pass-pipeline/06-04-SUMMARY.md when done.
</output>
