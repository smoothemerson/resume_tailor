---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_cli.py
autonomous: true
requirements:
  - TEST-10
  - TEST-11
must_haves:
  truths:
    - "pytest -m e2e with Ollama stopped exits 0: TEST-10 passes, TEST-11 is SKIPPED (not FAILED)"
    - "pytest -m e2e with Ollama running exits 0: both TEST-10 and TEST-11 pass"
    - "TEST-10 subprocess exits returncode 1 and produces 'Error: Job description cannot be empty.' on stderr"
    - "TEST-11 subprocess exits returncode 0, stdout contains 'Tailored resume written to:', and one tailored_resume_YYYYMMDD_HHMMSS.tex file exists in tmp_path"
  artifacts:
    - path: "tests/e2e/test_cli.py"
      provides: "E2E tests TEST-10 and TEST-11 (D-09)"
      contains: "@pytest.mark.e2e"
    - path: "tests/e2e/test_cli.py"
      provides: "module-level CLI_PATH and MINIMAL_RESUME constants (D-01, D-03)"
      contains: "CLI_PATH = Path(__file__).parents[2]"
  key_links:
    - from: "tests/e2e/test_cli.py"
      to: "src/cli.py"
      via: "subprocess.run([sys.executable, CLI_PATH, ...])"
      pattern: "CLI_PATH = Path.*parents\\[2\\].*src.*cli\\.py"
    - from: "tests/e2e/test_cli.py"
      to: "tests/conftest.py"
      via: "require_ollama fixture argument in TEST-11 only"
      pattern: "def test_golden_path.*require_ollama"
---

<objective>
Create tests/e2e/test_cli.py with two E2E tests that invoke the CLI as a subprocess.

Purpose: Verify observable CLI behavior — exit codes, stderr messages, stdout output, and output file creation — as a black-box subprocess invocation. This validates the full pipeline end-to-end, covering behavior that unit and integration tests cannot observe (argparse wiring, sys.exit paths, file system output).

Output: tests/e2e/test_cli.py implementing TEST-10 (D-01 through D-05, D-07, D-09) and TEST-11 (D-01 through D-04, D-06, D-08, D-09).
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/11-e2e-tests/11-CONTEXT.md
@.planning/phases/11-e2e-tests/11-RESEARCH.md
@.planning/phases/11-e2e-tests/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/e2e/test_cli.py with TEST-10 and TEST-11</name>
  <files>tests/e2e/test_cli.py</files>
  <read_first>
    - src/cli.py — confirm exact empty-JD error string (line 45: "Error: Job description cannot be empty."), exact success stdout string (line 59: "Tailored resume written to:"), and --resume / --output-dir argparse flags (lines 20-21)
    - tests/conftest.py — confirm require_ollama fixture signature and scope (session-scoped, calls pytest.skip when Ollama unavailable)
    - tests/integration/test_llm_client.py — MINIMAL_RESUME constant shape and import block as structural analog
    - tests/unit/test_resume_writer.py — re.match filename pattern (r"tailored_resume_\d{8}_\d{6}\.tex") used in golden-path assertion
  </read_first>
  <action>
    Create tests/e2e/test_cli.py as a new file. The file must follow this structure exactly.

    Module-level imports (per D-01, D-02, D-03): import re, subprocess, sys, pathlib.Path, pytest. No other imports.

    Module-level constants before all test functions:
    - CLI_PATH = Path(__file__).parents[2] / "src" / "cli.py" — per D-01; parents[2] is repo root (parents[0]=tests/e2e, parents[1]=tests, parents[2]=workspace root)
    - MINIMAL_RESUME = concatenated string with \\documentclass{article}, \\begin{document}, \\section{Summary}, "AI engineer with 3 years experience.", \\end{document} — per D-03; same shape as tests/integration/test_llm_client.py; must NOT be imported from that file

    TEST-10 function (per D-05, D-07, D-09):
    - Name: test_empty_jd_exits_1_with_stderr_message
    - Decorator: @pytest.mark.e2e only — NO require_ollama fixture (per D-07; this test must run without Ollama)
    - Body: subprocess.run([sys.executable, CLI_PATH], input="END\n", capture_output=True, text=True) — per D-02
    - Assertions: result.returncode == 1 and "Error: Job description cannot be empty." in result.stderr — per D-05; exact string match

    TEST-11 function (per D-04, D-06, D-08, D-09):
    - Name: test_golden_path_exits_0_creates_output_file
    - Decorator: @pytest.mark.e2e
    - Function parameters: require_ollama, tmp_path — per D-08; require_ollama as positional arg triggers skip when Ollama is down
    - Body:
      1. Write MINIMAL_RESUME to tmp_path / "resume.tex" with encoding="utf-8" — per D-04
      2. subprocess.run([sys.executable, CLI_PATH, "--resume", str(resume_file), "--output-dir", str(tmp_path)], input="Python backend engineer with REST API experience\nEND\n", capture_output=True, text=True) — per D-02, D-04
    - Assertions per D-06:
      1. result.returncode == 0
      2. "Tailored resume written to:" in result.stdout
      3. output_files = list(tmp_path.glob("tailored_resume_*.tex")); assert len(output_files) == 1
      4. assert re.match(r"tailored_resume_\d{8}_\d{6}\.tex", output_files[0].name) — no content assertions (LaTeX correctness covered by Phase 10 integration tests)

    Critical correctness constraints:
    - TEST-10 must have NO fixture arguments (not even tmp_path) — its subprocess.run call needs no --resume flag because cli.py falls back to BASE_RESUME_PATH from config, and the test exits before reading the file
    - TEST-11 output_files glob must search tmp_path, not a hardcoded path, because --output-dir str(tmp_path) redirects cli.py output there (per D-04)
    - Both test functions carry @pytest.mark.e2e — pyproject.toml has --strict-markers active; missing this decorator causes a collection error
    - No cwd= argument in either subprocess.run call — /workspace/src is on sys.path via the editable install .pth file
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest tests/e2e/test_cli.py::test_empty_jd_exits_1_with_stderr_message -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/test_cli.py exists
    - tests/e2e/test_cli.py contains CLI_PATH = Path(__file__).parents[2] / "src" / "cli.py"
    - tests/e2e/test_cli.py contains MINIMAL_RESUME
    - tests/e2e/test_cli.py contains @pytest.mark.e2e on both test functions
    - tests/e2e/test_cli.py contains def test_empty_jd_exits_1_with_stderr_message() with NO fixture parameters
    - tests/e2e/test_cli.py contains def test_golden_path_exits_0_creates_output_file(require_ollama, tmp_path)
    - uv run pytest tests/e2e/test_cli.py::test_empty_jd_exits_1_with_stderr_message -v exits 0 with PASSED
    - uv run pytest tests/e2e/test_cli.py -m e2e -v exits 0: TEST-10 is PASSED, TEST-11 is PASSED or SKIPPED (depending on Ollama availability)
    - uv run pytest tests/ -v exits 0 with full suite green
  </acceptance_criteria>
  <done>tests/e2e/test_cli.py exists with TEST-10 passing and TEST-11 passing-or-skipping. pytest -m e2e exits 0. Full suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child process | subprocess.run spawns cli.py as a child; stdin is controlled literal strings, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | CLI_PATH derivation | accept | CLI_PATH derived from __file__ (not user input); off-by-one in parents[] index would cause FileNotFoundError at test runtime, not a security risk |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; stdlib + existing pytest only |
</threat_model>

<verification>
Run in order:

1. TEST-10 in isolation: `uv run pytest tests/e2e/test_cli.py::test_empty_jd_exits_1_with_stderr_message -v`
   Expected: PASSED, exit 0

2. Full e2e marker: `uv run pytest tests/e2e/test_cli.py -m e2e -v`
   Expected with Ollama absent: TEST-10 PASSED, TEST-11 SKIPPED, exit 0
   Expected with Ollama running: both PASSED, exit 0

3. Full suite: `uv run pytest tests/ -v`
   Expected: All prior tests remain green; no regressions
</verification>

<success_criteria>
1. uv run pytest tests/e2e/test_cli.py::test_empty_jd_exits_1_with_stderr_message -v exits 0 (TEST-10 always passes, no Ollama needed)
2. uv run pytest tests/e2e/ -m e2e -v exits 0 with TEST-11 PASSED or SKIPPED depending on Ollama availability
3. uv run pytest tests/ -v exits 0 — full suite green, no regressions
</success_criteria>

<output>
Create .planning/phases/11-e2e-tests/11-01-SUMMARY.md when done
</output>
