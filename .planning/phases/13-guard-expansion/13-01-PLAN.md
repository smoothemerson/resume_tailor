---
phase: 13-guard-expansion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/guards.py
  - tests/unit/test_guards.py
autonomous: true
requirements:
  - GARD-05
  - TEST-12
must_haves:
  truths:
    - "_check_technology_substitution warns when technologies are substituted (removed AND added)"
    - "_check_technology_substitution warns when technologies are only removed"
    - "_check_technology_substitution warns when technologies are only added"
    - "_check_technology_substitution is silent when Skills section is identical"
    - "_check_technology_substitution is silent when no Skills section exists in either text"
    - "_check_technology_substitution never raises on malformed (None) input"
  artifacts:
    - path: "src/guards.py"
      provides: "_check_technology_substitution function"
      contains: "def _check_technology_substitution"
    - path: "tests/unit/test_guards.py"
      provides: "Unit tests for GARD-05 and TEST-12"
      contains: "@pytest.mark.unit"
  key_links:
    - from: "tests/unit/test_guards.py"
      to: "src/guards.py"
      via: "from guards import _check_technology_substitution"
      pattern: "from guards import _check_technology_substitution"
    - from: "_check_technology_substitution"
      to: "guards.logger"
      via: "logger.warning()"
      pattern: "logger\\.warning"
---

<objective>
Implement `_check_technology_substitution(original, tailored)` in `src/guards.py` per GARD-05, and create `tests/unit/test_guards.py` with all six TEST-12 test cases covering that guard.

Purpose: Detect when the LLM has swapped one named technology for another in the Skills section — the most common form of hallucination in resume tailoring. The guard emits three distinct warning variants: substitution (removed AND added), removal-only, and addition-only.

Output: `_check_technology_substitution` in `src/guards.py`; `tests/unit/test_guards.py` with six `@pytest.mark.unit` tests covering TEST-12 cases.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-guard-expansion/13-CONTEXT.md
@.planning/phases/13-guard-expansion/13-RESEARCH.md
@.planning/phases/13-guard-expansion/13-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write failing tests for _check_technology_substitution (RED)</name>
  <files>tests/unit/test_guards.py</files>
  <read_first>
    - src/guards.py — current state; confirms _check_technology_substitution does not yet exist
    - src/guards_test.py — shows patch("guards.logger") pattern, call_args_list + any() idiom, and never-raise test shape
    - tests/unit/test_resume_writer.py — shows @pytest.mark.unit decorator usage and pytest function style
    - pyproject.toml — confirms pythonpath = ["src"], --import-mode=importlib, --strict-markers; no sys.path.insert needed
  </read_first>
  <behavior>
    - test_technology_substitution_warns_on_substitution: original has "Python, Java", tailored has "Python, Go" (Java removed, Go added); assert warning call_args_list contains "Java" AND "Go"
    - test_technology_substitution_warns_on_removal_only: original has "Python, Java", tailored has "Python" only; assert warning call_args_list contains "Java"
    - test_technology_substitution_warns_on_addition_only: original has "Python", tailored has "Python, Go"; assert warning call_args_list contains "Go"
    - test_technology_substitution_silent_for_identical: both original and tailored have "Python, Java"; assert mock_logger.warning.assert_not_called()
    - test_technology_substitution_silent_for_no_skills_section: both texts have no \header{Skills}; assert mock_logger.warning.assert_not_called()
    - test_technology_substitution_malformed_input_no_raise: call _check_technology_substitution(None, None); must not raise any exception
  </behavior>
  <action>
    Create tests/unit/test_guards.py (new file — does not exist yet).

    Import block: `import pytest`, `from unittest.mock import patch`, `from guards import _check_technology_substitution`.

    Every test function is decorated with `@pytest.mark.unit`. No `__init__.py` in tests/unit/.

    Fixture shape for Skills tests: use raw-string literals for the LaTeX skeleton, e.g., a string containing `\header{Skills}\n{csv_content}\n\header{Education}` for both original and tailored arguments.

    For "warns" tests: use `with patch("guards.logger") as mock_logger:`, then call `_check_technology_substitution(original, tailored)`, then build `calls = [str(c) for c in mock_logger.warning.call_args_list]` and use `assert any("Java" in c for c in calls)` style assertions (per D-09 and the call_args_list pattern from src/guards_test.py).

    For "silent" tests: use `mock_logger.warning.assert_not_called()` after the call (per D-04 and D-09).

    For "malformed_input_no_raise" test: call `_check_technology_substitution(None, None)` with no patch — the test passes if no exception is raised (per D-13).

    All six tests must FAIL at collection time because `_check_technology_substitution` does not yet exist in `src/guards.py`. Run `pytest -m unit tests/unit/test_guards.py -x` and confirm ImportError or AttributeError.
  </action>
  <verify>
    <automated>pytest -m unit tests/unit/test_guards.py -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_guards.py exists
    - File contains exactly six test functions: test_technology_substitution_warns_on_substitution, test_technology_substitution_warns_on_removal_only, test_technology_substitution_warns_on_addition_only, test_technology_substitution_silent_for_identical, test_technology_substitution_silent_for_no_skills_section, test_technology_substitution_malformed_input_no_raise
    - Each function is decorated with @pytest.mark.unit
    - Import line reads: from guards import _check_technology_substitution
    - Running pytest -m unit tests/unit/test_guards.py -x fails with ImportError or collection error (RED state confirmed — function not yet in guards.py)
  </acceptance_criteria>
  <done>tests/unit/test_guards.py exists with six @pytest.mark.unit tests that currently fail because the implementation does not exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement _check_technology_substitution in guards.py (GREEN)</name>
  <files>src/guards.py</files>
  <read_first>
    - src/guards.py — read the full file; understand _EMPLOYER_PATTERN, _check_missing_sections (for \header{} boundary pattern), the never-raise try/except wrapper shape, and the existing imports
    - .planning/phases/13-guard-expansion/13-RESEARCH.md — Pattern 1 (section extraction), Pattern 2 (macro stripping + tokenization), Pattern 3 (three-way warning logic), Pattern 7 (never-raise wrapper)
    - .planning/phases/13-guard-expansion/13-CONTEXT.md — D-01 through D-04, D-09, D-13
    - tests/unit/test_guards.py — read the test file just created; implementation must make all six tests pass
  </read_first>
  <behavior>
    - _check_technology_substitution(original, tailored) -> None
    - Extracts Skills section body using \header{Skills} as the start boundary and the next \header{...} or end-of-string as the end boundary (per D-01); uses re.search with re.DOTALL
    - If \header{Skills} is absent from either original or tailored, returns silently with no warning (per D-04)
    - Strips LaTeX macros via re.sub(r'\\[a-zA-Z]+\{([^}]*)\}', r'\1', section_text) before tokenizing (per D-03)
    - Tokenizes by splitting on comma and stripping whitespace: {t.strip() for t in cleaned.split(',') if t.strip()} (per D-02)
    - Computes removed = original_techs - tailored_techs and added = tailored_techs - original_techs
    - If removed and added: logger.warning message names both sorted(removed) and sorted(added) (per D-09 substitution case)
    - If removed only: logger.warning message names sorted(removed) (per D-09 removal-only case)
    - If added only: logger.warning message names sorted(added) (per D-09 addition-only case)
    - If neither removed nor added: silent (identical skills or empty skills)
    - Entire function body is wrapped in try/except Exception as exc: logger.warning(f"Technology substitution check failed: {exc}") (per D-13)
  </behavior>
  <action>
    Add `_check_technology_substitution(original: str, tailored: str) -> None` to src/guards.py, after the existing `_check_hallucinated_employers` function and before `run_guards`.

    No new imports are needed — `re` and `logger` are already imported at the top of guards.py.

    Internal helper pattern for section extraction uses rf'\\header\{{{re.escape(section_name)}\}}(.*?)(?=\\header\{{|$)' with re.DOTALL (see RESEARCH.md Pattern 1). Implement this as an inline pattern or a module-level private helper — either is acceptable so long as the same pattern is also usable by Task 3 in Plan 02 (_check_protected_sections). Prefer extracting a module-private helper `_extract_section(text: str, section_name: str) -> str | None` so Plan 02 can reuse it; place it above `_check_technology_substitution`.

    Technology extraction strips LaTeX macros then splits on commas (see RESEARCH.md Pattern 2). Implement inline within `_check_technology_substitution` or in a separate `_extract_technologies(section_text: str) -> set[str]` helper.

    Warning message strings must contain the technology names so the tests' `any("Java" in c ...)` assertions can match. The exact format from CONTEXT.md D-09 uses sorted() lists in the message, for example: f"Technology substitution in Skills: removed {sorted(removed)}, added {sorted(added)}".

    After implementing, run `pytest -m unit tests/unit/test_guards.py -x` to confirm all six tests pass (GREEN).

    Note: run_guards() is NOT modified in this plan — GARD-07 is addressed in Plan 03.
  </action>
  <verify>
    <automated>pytest -m unit tests/unit/test_guards.py::test_technology_substitution_warns_on_substitution tests/unit/test_guards.py::test_technology_substitution_warns_on_removal_only tests/unit/test_guards.py::test_technology_substitution_warns_on_addition_only tests/unit/test_guards.py::test_technology_substitution_silent_for_identical tests/unit/test_guards.py::test_technology_substitution_silent_for_no_skills_section tests/unit/test_guards.py::test_technology_substitution_malformed_input_no_raise -v</automated>
  </verify>
  <acceptance_criteria>
    - src/guards.py contains def _check_technology_substitution(
    - src/guards.py contains def _extract_section( (or equivalent inline regex pattern — the function name is flexible, but the section extraction logic must use re.DOTALL and a \header{} lookahead boundary)
    - All six test_technology_substitution_* tests in tests/unit/test_guards.py pass
    - pytest -m unit tests/unit/test_guards.py -x exits 0
    - Existing tests in src/guards_test.py still pass: pytest src/guards_test.py exits 0
  </acceptance_criteria>
  <done>All six _check_technology_substitution unit tests pass; existing guard tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> guard input | Strings produced by Ollama passed to _check_technology_substitution; content is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial of Service | _check_technology_substitution | mitigate | try/except Exception wrapper (D-13) catches any regex error or unexpected type and logs warning; guard never raises |
| T-13-02 | Tampering | guards.py — _EMPLOYER_PATTERN, section extraction | accept | Pure text comparison; no state mutation; no external calls; output is only logger.warning() |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all implementation uses stdlib re + existing project deps |
</threat_model>

<verification>
After completing both tasks:
- `pytest -m unit tests/unit/test_guards.py -x` exits 0 (all six TEST-12 tests pass)
- `pytest src/guards_test.py` exits 0 (existing tests unaffected)
- `ruff check src/guards.py` exits 0 (no lint errors)
- `grep -c "def _check_technology_substitution" src/guards.py` outputs 1
</verification>

<success_criteria>
- `_check_technology_substitution` is defined in src/guards.py with the never-raise wrapper (D-13)
- All six @pytest.mark.unit tests in tests/unit/test_guards.py pass for GARD-05 and TEST-12
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/13-guard-expansion/13-01-SUMMARY.md` when done.
</output>
