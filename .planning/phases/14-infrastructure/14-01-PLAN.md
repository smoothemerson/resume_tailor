---
phase: "14-infrastructure"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PKG-01
  - CI-01
must_haves:
  truths:
    - "uv build wheel includes jd_analyzer.py and keyword_matcher.py"
    - "CI workflow triggers on push and pull_request to main with no secrets references"
    - "CI runs ruff check src/, ruff format --check src/, and pytest -m unit"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated wheel include list with jd_analyzer.py and keyword_matcher.py"
      contains: "src/jd_analyzer.py"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "pytest -m unit"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "uv sync reads pyproject.toml for dev dependencies (pytest, ruff)"
      pattern: "uv sync"
---

<objective>
Fix the packaging gap in pyproject.toml so the wheel includes jd_analyzer.py and keyword_matcher.py (PKG-01), and create the GitHub Actions CI workflow that lints and runs unit tests on every push and PR to main (CI-01).

Purpose: Two modules shipped in v1.1 (jd_analyzer.py, keyword_matcher.py) are absent from the wheel include list — any pip install of the package would be missing them. The CI workflow closes the gap where the project has no automated quality gate.

Output: Updated pyproject.toml with 10-entry include list; new .github/workflows/ci.yml with checkout, uv setup, lint, and test steps.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-infrastructure/14-CONTEXT.md
@.planning/phases/14-infrastructure/14-RESEARCH.md
@.planning/phases/14-infrastructure/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jd_analyzer.py and keyword_matcher.py to wheel include list (PKG-01)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — read the entire file; the [tool.hatch.build.targets.wheel] include list is lines 16-27; understand the exact existing entries and alphabetical ordering before editing
  </read_first>
  <action>
    Edit the [tool.hatch.build.targets.wheel] include list in pyproject.toml to add "src/jd_analyzer.py" and "src/keyword_matcher.py" in alphabetical position (after "src/guards.py", before "src/llm_client.py"). Per D-01 and PKG-01, path format must use the "src/X.py" form (root-relative, matching all existing entries). Do not touch any other section of pyproject.toml. The final include list must be 10 entries total in alphabetical order: cli.py, config.py, diff_view.py, guards.py, jd_analyzer.py, keyword_matcher.py, llm_client.py, log_manager.py, resume_reader.py, resume_writer.py — all prefixed with "src/".
  </action>
  <verify>
    <automated>grep -c "src/jd_analyzer.py" pyproject.toml && grep -c "src/keyword_matcher.py" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the line `    "src/jd_analyzer.py",`
    - pyproject.toml contains the line `    "src/keyword_matcher.py",`
    - The include list has exactly 10 entries (grep -c '"src/' pyproject.toml returns 10 within the include block)
    - All other pyproject.toml content is unchanged (requires-python, dependencies, scripts, pytest config — all identical to pre-edit state)
  </acceptance_criteria>
  <done>grep output returns "1" for both jd_analyzer.py and keyword_matcher.py lines; pyproject.toml [tool.hatch.build.targets.wheel] include has 10 entries in alphabetical order</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow (CI-01, D-05 through D-08)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/14-infrastructure/14-RESEARCH.md — Pattern 3 (lines 186-221) is the authoritative CI YAML template; read the critical version note on astral-sh/setup-uv@v8.2.0 before writing
    - .planning/phases/14-infrastructure/14-PATTERNS.md — .github/workflows/ci.yml section confirms no codebase analog exists; use RESEARCH.md pattern directly
  </read_first>
  <action>
    Create the directory .github/workflows/ and write .github/workflows/ci.yml with these exact properties (per D-05 through D-08 and CI-01):

    - name: CI
    - on: push branches [main] and pull_request branches [main]
    - jobs.test.runs-on: ubuntu-latest
    - Step 1: actions/checkout@v4 (no name needed, uses only)
    - Step 2: name "Install uv and Python 3.13", uses astral-sh/setup-uv@v8.2.0, with python-version "3.13"
    - Step 3: name "Install dependencies", run: uv sync
    - Step 4: name "Lint", run multiline: uv run ruff check src/ then uv run ruff format --check src/ (per D-06)
    - Step 5: name "Test", run: uv run pytest -m unit

    CRITICAL: Use astral-sh/setup-uv@v8.2.0 — NOT @v4 (stale tag; Astral stopped publishing moving major-version tags at v8.0.0, March 2025).
    CRITICAL: The workflow must contain zero ${{ secrets.* }} references (CI-01 security requirement — this workflow calls only public tools and needs no external credentials).
  </action>
  <verify>
    <automated>grep -c "astral-sh/setup-uv@v8.2.0" .github/workflows/ci.yml && grep -c "pytest -m unit" .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and is valid YAML (python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0)
    - File contains "astral-sh/setup-uv@v8.2.0" (not @v4 or any other tag)
    - File contains "actions/checkout@v4"
    - File contains "python-version" with value "3.13"
    - File contains "uv sync" (dependency install step)
    - File contains "uv run ruff check src/" (lint step)
    - File contains "uv run ruff format --check src/" (format check step per D-06)
    - File contains "uv run pytest -m unit" (test step)
    - File does NOT contain "${{ secrets." anywhere (grep -c "secrets\." .github/workflows/ci.yml returns 0)
    - on: block triggers on both push and pull_request to branches: [main]
  </acceptance_criteria>
  <done>.github/workflows/ci.yml exists, is valid YAML, references setup-uv@v8.2.0, runs lint + format-check + pytest -m unit, and contains no secrets references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Actions marketplace | GitHub fetches action code from astral-sh/setup-uv and actions/checkout repos |
| pyproject.toml → hatchling build | Build system reads include list to determine wheel contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | .github/workflows/ci.yml | mitigate | Pin actions to full semver tags (astral-sh/setup-uv@v8.2.0, actions/checkout@v4) rather than moving tags to prevent supply-chain substitution |
| T-14-02 | Information Disclosure | .github/workflows/ci.yml | accept | Workflow contains no secrets, tokens, or credentials — only public tool invocations; no disclosure risk |
| T-14-03 | Tampering | pyproject.toml include list | accept | Include list controls wheel contents, not runtime behavior; wrong entries cause missing modules (packaging bug) not security vulnerability |
| T-14-SC | Tampering | GitHub Actions installs | mitigate | Both actions are Astral-official and GitHub-official; Package Legitimacy Audit in RESEARCH.md confirmed [VERIFIED] status for both |
</threat_model>

<verification>
After both tasks complete:
1. `grep "src/jd_analyzer.py" pyproject.toml` outputs the entry
2. `grep "src/keyword_matcher.py" pyproject.toml` outputs the entry
3. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
4. `grep "secrets\." .github/workflows/ci.yml` returns nothing (exit 1 = pass)
5. `grep "setup-uv@v8.2.0" .github/workflows/ci.yml` confirms correct action version
</verification>

<success_criteria>
- pyproject.toml [tool.hatch.build.targets.wheel] include list contains exactly 10 entries including src/jd_analyzer.py and src/keyword_matcher.py
- .github/workflows/ci.yml exists, is valid YAML, triggers on push and pull_request to main, uses ubuntu-latest + Python 3.13, runs ruff check + ruff format --check + pytest -m unit, contains no ${{ secrets.* }} references
</success_criteria>

<output>
Create `.planning/phases/14-infrastructure/14-01-SUMMARY.md` when done
</output>
