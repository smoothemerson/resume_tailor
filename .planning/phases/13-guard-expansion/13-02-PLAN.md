---
phase: 13-guard-expansion
plan: 02
type: tdd
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/guards.py
  - tests/unit/test_guards.py
autonomous: true
requirements:
  - GARD-06
  - TEST-13
must_haves:
  truths:
    - "_check_protected_sections warns when the contact block differs between original and tailored"
    - "_check_protected_sections warns when the Education section content differs"
    - "_check_protected_sections warns when the Languages section content differs"
    - "_check_protected_sections warns when an employer header line (via _EMPLOYER_PATTERN) is changed or removed"
    - "_check_protected_sections is silent when all elements match"
    - "_check_protected_sections never raises on empty string input"
  artifacts:
    - path: "src/guards.py"
      provides: "_check_protected_sections function"
      contains: "def _check_protected_sections"
    - path: "tests/unit/test_guards.py"
      provides: "Unit tests for GARD-06 and TEST-13"
      contains: "test_protected_sections"
  key_links:
    - from: "_check_protected_sections"
      to: "_extract_section"
      via: "reuses module-private helper from Plan 01"
      pattern: "_extract_section"
    - from: "_check_protected_sections"
      to: "_EMPLOYER_PATTERN"
      via: "_EMPLOYER_PATTERN.findall(original)"
      pattern: "_EMPLOYER_PATTERN\\.findall"
---

<objective>
Implement `_check_protected_sections(original, tailored)` in `src/guards.py` per GARD-06, and extend `tests/unit/test_guards.py` with all six TEST-13 test cases covering that guard.

Purpose: Detect mutations to protected resume elements — contact block, Education section, Languages section, employer header macros, and project anchors — that the LLM must never alter. One warning per changed element (per D-10).

Output: `_check_protected_sections` added to `src/guards.py`; six additional `@pytest.mark.unit` tests appended to `tests/unit/test_guards.py`.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-guard-expansion/13-CONTEXT.md
@.planning/phases/13-guard-expansion/13-RESEARCH.md
@.planning/phases/13-guard-expansion/13-PATTERNS.md
@.planning/phases/13-guard-expansion/13-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write failing tests for _check_protected_sections (RED)</name>
  <files>tests/unit/test_guards.py</files>
  <read_first>
    - tests/unit/test_guards.py — read current state (six tests from Plan 01 exist); append six new tests for _check_protected_sections
    - src/guards.py — confirms _check_protected_sections does not yet exist (only _check_technology_substitution was added in Plan 01)
    - .planning/phases/13-guard-expansion/13-RESEARCH.md — Pattern 4 (contact block \begin{center}...\end{center}), Pattern 5 (project anchor \href{url}{\textbf{Name}}), Pattern 6 (employer header _EMPLOYER_PATTERN), D-06 (Education/Languages section comparison), D-10 (one warning per changed element)
    - .planning/phases/13-guard-expansion/13-CONTEXT.md — D-05 through D-10, D-12
  </read_first>
  <behavior>
    - test_protected_sections_warns_on_contact_diff: original has \begin{center}Name A\end{center}, tailored has \begin{center}Name B\end{center}; assert warning call_args_list contains a string matching "Contact" or "contact"
    - test_protected_sections_warns_on_education_diff: original has \header{Education}BSc Computer Science\header{Languages}, tailored has \header{Education}BSc Data Science\header{Languages}; assert warning call_args_list contains a string matching "Education"
    - test_protected_sections_warns_on_languages_diff: original has \header{Languages}English\header{Experience}, tailored has \header{Languages}French\header{Experience}; assert warning call_args_list contains a string matching "Languages"
    - test_protected_sections_warns_on_employer_header_change: original has \employer{Acme Corp}{2022}{Engineer}, tailored has \employer{Beta Corp}{2022}{Engineer}; assert warning call_args_list contains a string matching "Acme Corp"
    - test_protected_sections_silent_when_unchanged: original and tailored are identical strings containing \begin{center}Same\end{center}, \header{Education}same\header{Languages}same, \employer{Same}{2022}{Engineer}; assert mock_logger.warning.assert_not_called()
    - test_protected_sections_empty_strings_no_raise: call _check_protected_sections("", "") with no patch; must not raise any exception
  </behavior>
  <action>
    Append six new test functions to tests/unit/test_guards.py (the file already contains six tests for _check_technology_substitution from Plan 01).

    Update the import line to include _check_protected_sections: `from guards import _check_technology_substitution, _check_protected_sections`.

    Use inline LaTeX strings as test fixtures — no separate fixture functions needed. Raw string literals are preferred for LaTeX content (avoids double-escaping backslashes).

    For contact block test: build a minimal string with `\begin{center}` and `\end{center}` — just the markers and a name/email line. The guard must detect when that block's content differs.

    For Education and Languages tests: build strings using `\header{Education}...content...\header{NextSection}` shape. The content between the headers is what differs.

    For employer header test: use `\employer{Acme Corp}{2022}{Engineer}` in original and `\employer{Beta Corp}{2022}{Engineer}` in tailored. The guard uses `_EMPLOYER_PATTERN` (already compiled as a module-level constant in guards.py) and warns when a tuple from original is absent in tailored.

    For silent test: ensure original == tailored for all checked elements. Pass identical strings to both arguments.

    For empty-strings no-raise test: call directly without patch — the test passes if execution completes without raising.

    All six new tests must FAIL at collection time (ImportError for _check_protected_sections). Run `pytest -m unit tests/unit/test_guards.py -x` to confirm RED state — the existing six tests still pass, the six new ones fail.
  </action>
  <verify>
    <automated>pytest -m unit tests/unit/test_guards.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_guards.py now contains twelve test functions total (six from Plan 01 + six new)
    - Import line includes _check_protected_sections
    - The six new test functions are named: test_protected_sections_warns_on_contact_diff, test_protected_sections_warns_on_education_diff, test_protected_sections_warns_on_languages_diff, test_protected_sections_warns_on_employer_header_change, test_protected_sections_silent_when_unchanged, test_protected_sections_empty_strings_no_raise
    - Each new test is decorated with @pytest.mark.unit
    - The original six Plan-01 tests still pass; new six tests fail (RED state confirmed)
  </acceptance_criteria>
  <done>tests/unit/test_guards.py has twelve tests total; six Plan-01 tests green, six new tests red (ImportError or AttributeError for _check_protected_sections).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement _check_protected_sections in guards.py (GREEN)</name>
  <files>src/guards.py</files>
  <read_first>
    - src/guards.py — read full current state; note the _extract_section helper added in Plan 01, _EMPLOYER_PATTERN, and the never-raise wrapper shape
    - .planning/phases/13-guard-expansion/13-RESEARCH.md — Pattern 4 (contact block: r'\\begin\{center\}(.*?)\\end\{center\}' with re.DOTALL), Pattern 5 (project anchors: r'\\href\{([^}]+)\}\{\\textbf\{([^}]+)\}\}'), Pattern 6 (_EMPLOYER_PATTERN reuse), D-06, D-07, D-08, D-10
    - .planning/phases/13-guard-expansion/13-CONTEXT.md — D-05 through D-10, D-13
    - tests/unit/test_guards.py — read the six new failing tests to understand what warning strings and behaviors are expected
  </read_first>
  <behavior>
    - _check_protected_sections(original, tailored) -> None
    - Contact block: extract first \begin{center}...\end{center} using re.search(r'\\begin\{center\}(.*?)\\end\{center\}', text, re.DOTALL); if content differs, emit logger.warning("Contact block was modified in tailored output.") (per D-05, D-10)
    - Education section: use _extract_section(text, "Education") helper from Plan 01; if both are not None and differ after strip(), emit logger.warning("Education section was modified in tailored output.") (per D-06, D-10)
    - Languages section: use _extract_section(text, "Languages") helper; if both not None and differ after strip(), emit logger.warning("Languages section was modified in tailored output.") (per D-06, D-10)
    - Employer headers: original_headers = set(_EMPLOYER_PATTERN.findall(original)), tailored_headers = set(_EMPLOYER_PATTERN.findall(tailored)); for each header in original_headers - tailored_headers: emit logger.warning(f'Employer header changed or removed: "{header[0]}"') (per D-07, D-10)
    - Project anchors: pattern = r'\\href\{([^}]+)\}\{\\textbf\{([^}]+)\}\}'; compute original_projects = set(re.findall(pattern, original)), tailored_projects = set(re.findall(pattern, tailored)); for url, name in original_projects - tailored_projects: emit logger.warning(f'Project anchor changed or removed: "{name}" ({url})') (per D-08, D-10)
    - Entire function body is wrapped in try/except Exception as exc: logger.warning(f"Protected sections check failed: {exc}") (per D-13)
    - Each changed element emits exactly one warning (D-10) — do not aggregate or suppress
  </behavior>
  <action>
    Add `_check_protected_sections(original: str, tailored: str) -> None` to src/guards.py, after `_check_technology_substitution` and before `run_guards`.

    No new imports are needed — `re` and `logger` are already imported; `_EMPLOYER_PATTERN` and `_extract_section` (added in Plan 01) are module-level.

    Contact block extraction: use `re.search(r'\\begin\{center\}(.*?)\\end\{center\}', text, re.DOTALL)` and compare `.group(1).strip()` results. If either extraction yields None (no center block in text), skip the contact check silently.

    Education and Languages extraction: call `_extract_section(original, "Education")` and `_extract_section(tailored, "Education")`; compare `.strip()` values only when both are not None.

    Employer header comparison: set-diff `original_headers - tailored_headers` using `set(_EMPLOYER_PATTERN.findall(...))` — if the result is non-empty, emit one warning per missing header using `header[0]` (the employer name from the three-tuple match group).

    Project anchor comparison: extract URL+name tuples using `re.findall(r'\\href\{([^}]+)\}\{\\textbf\{([^}]+)\}\}', text)`; compute set-diff; emit one warning per (url, name) pair that was in original but absent in tailored.

    After implementing, run `pytest -m unit tests/unit/test_guards.py` to confirm all twelve tests pass.
  </action>
  <verify>
    <automated>pytest -m unit tests/unit/test_guards.py -v</automated>
  </verify>
  <acceptance_criteria>
    - src/guards.py contains def _check_protected_sections(
    - All twelve tests in tests/unit/test_guards.py pass (six from Plan 01 + six new)
    - pytest -m unit tests/unit/test_guards.py exits 0
    - pytest src/guards_test.py exits 0 (existing tests unaffected)
    - ruff check src/guards.py exits 0
    - src/guards.py contains the string "Contact block was modified" (warning message for D-05)
    - src/guards.py contains the string "Education section was modified" (warning message for D-06)
    - src/guards.py contains the string "Languages section was modified" (warning message for D-06)
    - src/guards.py contains the string "Employer header changed or removed" (warning message for D-07)
    - src/guards.py contains the string "Project anchor changed or removed" (warning message for D-08)
  </acceptance_criteria>
  <done>All twelve unit tests pass; _check_protected_sections is implemented with the never-raise wrapper; all existing tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> guard input | Strings produced by Ollama passed to _check_protected_sections; content is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Denial of Service | _check_protected_sections | mitigate | try/except Exception wrapper (D-13) catches regex errors, None inputs, and unexpected types; guard never raises |
| T-13-04 | Tampering | _check_protected_sections — contact block and section comparison | accept | Pure read-only text comparison; no state mutation; no external calls; output is only logger.warning() |
| T-13-05 | Information Disclosure | logger.warning() outputs | accept | Warnings are written to the local log only (log_manager); no external sink; PII in resume is already on disk |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all implementation uses stdlib re + existing project deps |
</threat_model>

<verification>
After completing both tasks:
- `pytest -m unit tests/unit/test_guards.py` exits 0 with 12 tests collected and passing
- `pytest src/guards_test.py` exits 0 (existing tests unaffected)
- `ruff check src/guards.py` exits 0
- `grep -c "def _check_protected_sections" src/guards.py` outputs 1
</verification>

<success_criteria>
- `_check_protected_sections` is defined in src/guards.py with the never-raise wrapper (D-13)
- All six @pytest.mark.unit tests for TEST-13 pass in tests/unit/test_guards.py
- All six Plan 01 tests continue to pass (no regressions)
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/13-guard-expansion/13-02-SUMMARY.md` when done.
</output>
