---
phase: 06-two-pass-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_jd_analyzer.py
  - tests/unit/test_llm_client.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "pytest -m unit tests/unit/test_jd_analyzer.py -x exits 0 after jd_analyzer.py is implemented"
    - "_build_messages with analysis=non-None produces user content containing <jd_analysis> tag"
    - "_build_messages with analysis=None produces user content with no <jd_analysis> tag"
    - "analyze_job_description returns None on JSON parse failure (not raises)"
    - "analyze_job_description raises RuntimeError on done_reason=length (not returns None)"
  artifacts:
    - path: "tests/unit/test_jd_analyzer.py"
      provides: "Unit tests for analyze_job_description — success, fallback, truncation paths"
      min_lines: 60
    - path: "tests/unit/test_llm_client.py"
      provides: "Two new _build_messages tests covering analysis injection and omission"
      contains: "test_build_messages_with_analysis_includes_jd_analysis_tag"
  key_links:
    - from: "tests/unit/test_jd_analyzer.py"
      to: "src/jd_analyzer.py"
      via: "from jd_analyzer import analyze_job_description"
      pattern: "from jd_analyzer import analyze_job_description"
    - from: "tests/unit/test_llm_client.py"
      to: "src/llm_client.py"
      via: "_build_messages called with 3 args"
      pattern: "_build_messages.*analysis"
---

<objective>
Create the test scaffold that must exist before any implementation runs. This plan produces the test files that Wave 2 implementation plans will make green.

Purpose: Prevents the "big bang green" anti-pattern where tests are written after implementation. Wave 1 tests start RED; Wave 2 turns them GREEN. Satisfies the Nyquist Wave 0 contract from 06-VALIDATION.md.

Output: tests/unit/test_jd_analyzer.py (new) and two new test functions appended to tests/unit/test_llm_client.py.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-CONTEXT.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-RESEARCH.md
@/workspace/.planning/phases/06-two-pass-pipeline/06-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create tests/unit/test_jd_analyzer.py with RED unit tests for analyze_job_description</name>
  <files>tests/unit/test_jd_analyzer.py</files>
  <read_first>
    - tests/unit/test_llm_client.py — exact mock/patch shape, MagicMock response construction, @pytest.mark.unit usage, jd_analyzer.requests.post patch path
    - src/llm_client.py — structure of generate_tailored_resume to understand the POST + done_reason guard + content extraction pattern being replicated in jd_analyzer
    - .planning/phases/06-two-pass-pipeline/06-PATTERNS.md — test_jd_analyzer.py section with exact mock response shapes for success, fallback, truncation, and missing-keys cases
    - .planning/phases/06-two-pass-pipeline/06-RESEARCH.md — Pitfall 4 (RuntimeError must re-raise), Pitfall 3 (fence-wrapped JSON)
  </read_first>
  <behavior>
    - test_analyze_job_description_returns_dict_with_three_keys: mock requests.post returning done_reason=stop and message.content='{"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}'; assert result is a dict with keys technologies, requirements, emphasis_areas
    - test_analyze_job_description_returns_none_on_json_parse_failure: mock response content to "not json at all"; assert result is None (must not raise)
    - test_analyze_job_description_returns_none_on_missing_required_keys: mock response content to '{"technologies": [], "requirements": []}' (missing emphasis_areas); assert result is None
    - test_analyze_job_description_returns_none_on_connection_error: mock requests.post to raise requests.ConnectionError; assert result is None (must not raise)
    - test_analyze_job_description_raises_runtime_error_on_truncation: mock response json returning done_reason=length; assert pytest.raises(RuntimeError, match="truncated")
    - test_analyze_job_description_returns_none_on_fence_wrapped_valid_json: mock response content to '```json\n{"technologies": ["Go"], "requirements": ["3 years"], "emphasis_areas": ["backend"]}\n```'; assert result is dict (fence stripping works), not None
  </behavior>
  <action>
    Create tests/unit/test_jd_analyzer.py. Import pattern: import pytest, import requests, from unittest.mock import MagicMock, patch, from jd_analyzer import analyze_job_description.

    Every test uses @pytest.mark.unit. Patch path is "jd_analyzer.requests.post" (NOT llm_client). No health check to patch — jd_analyzer does not call _check_ollama_health.

    Mock response construction (copy from tests/unit/test_llm_client.py lines 144-150 pattern):
    - mock_response = MagicMock()
    - mock_response.raise_for_status.return_value = None
    - mock_response.json.return_value = {"done_reason": "stop", "message": {"content": "..."}}
    - mock_post.return_value = mock_response

    For connection error test: @patch("jd_analyzer.requests.post") with mock_post.side_effect = requests.ConnectionError("refused"); call analyze_job_description("some jd"); assert result is None — no pytest.raises.

    For truncation test: set mock_response.json.return_value = {"done_reason": "length", "message": {"content": ""}} and assert pytest.raises(RuntimeError, match="truncated") — this is the one case that must raise, not return None.

    Tests are written RED (jd_analyzer.py does not exist yet) — pytest collect will report ImportError for this file until Plan 03 creates the module. That is the correct Wave 1 state.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/unit/test_jd_analyzer.py --collect-only 2>&1 | head -5; echo "Exit (collect-only not import-error check, module missing is expected RED)"</automated>
  </verify>
  <done>
    tests/unit/test_jd_analyzer.py exists and contains the six test functions listed in the behavior block. All use @pytest.mark.unit. Patch target is "jd_analyzer.requests.post". File is syntactically valid (python -m py_compile tests/unit/test_jd_analyzer.py exits 0).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _build_messages analysis injection tests to tests/unit/test_llm_client.py</name>
  <files>tests/unit/test_llm_client.py</files>
  <read_first>
    - tests/unit/test_llm_client.py — existing _build_messages tests at lines 14-61 to understand call shape and assertion style; existing imports to avoid duplicates
    - .planning/phases/06-two-pass-pipeline/06-PATTERNS.md — test_llm_client.py modifications section: two new test functions, exact names, call signatures, and assertions
  </read_first>
  <behavior>
    - test_build_messages_with_analysis_includes_jd_analysis_tag: call _build_messages("r", "jd", {"technologies": [], "requirements": [], "emphasis_areas": []}); assert "<jd_analysis>" in result[1]["content"] and "</jd_analysis>" in result[1]["content"]
    - test_build_messages_without_analysis_omits_jd_analysis_tag: call _build_messages("r", "jd") (2-arg, no analysis); assert "<jd_analysis>" not in result[1]["content"]
  </behavior>
  <action>
    Append two new test functions to the end of tests/unit/test_llm_client.py. Both use @pytest.mark.unit. No new imports needed — _build_messages is already imported at line 6.

    test_build_messages_with_analysis_includes_jd_analysis_tag calls _build_messages with three positional args: "r", "jd", and the dict {"technologies": [], "requirements": [], "emphasis_areas": []}. Asserts that result[1]["content"] contains both "<jd_analysis>" and "</jd_analysis>".

    test_build_messages_without_analysis_omits_jd_analysis_tag calls _build_messages with the existing 2-arg form: _build_messages("r", "jd"). Asserts that "<jd_analysis>" is NOT in result[1]["content"]. This test also proves backward compatibility — the existing 8 _build_messages tests remain valid with no modification.

    These tests start RED (TypeError: _build_messages() takes 2 positional arguments) until Plan 04 adds the analysis parameter. That is the correct Wave 1 state.
  </action>
  <verify>
    <automated>cd /workspace && python -m py_compile tests/unit/test_llm_client.py && echo "syntax OK"</automated>
  </verify>
  <done>
    tests/unit/test_llm_client.py contains test_build_messages_with_analysis_includes_jd_analysis_tag and test_build_messages_without_analysis_omits_jd_analysis_tag. Both use @pytest.mark.unit. Existing 8 _build_messages tests are untouched. python -m py_compile tests/unit/test_llm_client.py exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→module | Test files import from src modules; no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test patch targets | accept | Patch targets are string literals verified against module structure; incorrect paths cause ImportError/AttributeError caught immediately in CI |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — test-only additions to existing files |
</threat_model>

<verification>
python -m py_compile tests/unit/test_jd_analyzer.py exits 0
python -m py_compile tests/unit/test_llm_client.py exits 0
grep -c "def test_" tests/unit/test_jd_analyzer.py returns 6
grep "test_build_messages_with_analysis_includes_jd_analysis_tag\|test_build_messages_without_analysis_omits_jd_analysis_tag" tests/unit/test_llm_client.py shows both names
</verification>

<success_criteria>
tests/unit/test_jd_analyzer.py exists with 6 @pytest.mark.unit tests covering: success dict return, JSON parse failure returns None, missing keys returns None, connection error returns None, done_reason=length raises RuntimeError, fence-wrapped JSON succeeds.
tests/unit/test_llm_client.py gains 2 new tests covering _build_messages with and without analysis argument.
All files are syntactically valid. No existing tests are modified.
</success_criteria>

<output>
Create .planning/phases/06-two-pass-pipeline/06-01-SUMMARY.md when done.
</output>
