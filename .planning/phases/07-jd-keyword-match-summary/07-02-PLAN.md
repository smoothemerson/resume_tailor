---
phase: 07-jd-keyword-match-summary
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/cli.py
  - src/cli_test.py
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
must_haves:
  truths:
    - "cli.py calls show_keyword_match(analysis, result.content) after show_diff and before the final print, guarded by 'if analysis is not None' (per D-02, D-07)"
    - "show_keyword_match is NOT called when analysis is None (pass-1 failed case)"
    - "cli_test.py has a test asserting show_keyword_match is called when analysis is not None"
    - "All existing cli_test.py tests still pass after integration"
  artifacts:
    - path: "src/cli.py"
      provides: "Integrated keyword match summary call in main()"
      contains: "show_keyword_match"
    - path: "src/cli_test.py"
      provides: "Test asserting show_keyword_match called when analysis is not None"
      contains: "show_keyword_match"
  key_links:
    - from: "src/cli.py"
      to: "src/keyword_matcher.py"
      via: "from keyword_matcher import show_keyword_match"
      pattern: "from keyword_matcher import show_keyword_match"
    - from: "src/cli.py show_diff call"
      to: "src/cli.py show_keyword_match call"
      via: "if analysis is not None: guard"
      pattern: "if analysis is not None"
---

<objective>
Wire show_keyword_match into cli.py main() and update cli_test.py with the new integration test case. This is a three-line addition to cli.py and one new test function in cli_test.py. MATCH-01, MATCH-02, and MATCH-03 become exercised through the full CLI call stack after this plan.

Purpose: Deliver the end-to-end integration of the keyword match feature — the module exists after Plan 01; this plan connects it to the CLI entry point.

Output: Modified src/cli.py (import + call site), modified src/cli_test.py (new test + patching check for the existing analysis-success test).
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/phases/07-jd-keyword-match-summary/07-CONTEXT.md
@/workspace/.planning/phases/07-jd-keyword-match-summary/07-PATTERNS.md
@/workspace/.planning/phases/07-jd-keyword-match-summary/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire show_keyword_match into cli.py</name>
  <files>src/cli.py</files>

  <read_first>
    src/cli.py — current full file; read the import block (lines 1-11) and the main() try block (lines 48-62) before touching anything; the exact insertion points are the import block and the three lines after show_diff().
    .planning/phases/07-jd-keyword-match-summary/07-CONTEXT.md — D-02 (position: after show_diff, before final print), D-07 (caller uses "if analysis is not None:" guard before calling).
    .planning/phases/07-jd-keyword-match-summary/07-PATTERNS.md — cli.py integration point section showing the exact before/after code shape.
  </read_first>

  <behavior>
    - `from keyword_matcher import show_keyword_match` is imported in the project-local import block, inserted between `from jd_analyzer import analyze_job_description` and `from llm_client import TailorResult, generate_tailored_resume` (alphabetical order per D-06 pattern).
    - In main() try block, after `show_diff(resume_text, result.content)` and before `print(f"Tailored resume written to: ...")`, add exactly: `if analysis is not None:` on one line, `    show_keyword_match(analysis, result.content)` on the next line (4-space indent, inside the existing try block).
    - No other lines in cli.py are changed.
    - No comments added (CLAUDE.md: no inline comments).
  </behavior>

  <action>
    Edit src/cli.py in two locations:

    Location 1 — import block: Add `from keyword_matcher import show_keyword_match` after the line `from jd_analyzer import analyze_job_description` and before `from llm_client import TailorResult, generate_tailored_resume`.

    Location 2 — main() try block: Insert two lines after `show_diff(resume_text, result.content)` and before `print(f"Tailored resume written to: {output_path.resolve()}")`:
      Line 1: `        if analysis is not None:`
      Line 2: `            show_keyword_match(analysis, result.content)`

    Both edits use the exact indentation present in the file (4 spaces per level). Do not alter any surrounding lines.
  </action>

  <verify>
    <automated>/workspace/.venv/bin/pytest src/cli_test.py -x -q</automated>
  </verify>

  <done>
    - src/cli.py contains `from keyword_matcher import show_keyword_match` in the import block.
    - src/cli.py contains `if analysis is not None:` followed by `show_keyword_match(analysis, result.content)` after the show_diff call.
    - /workspace/.venv/bin/pytest src/cli_test.py -x -q exits 0 (all existing tests still pass).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add show_keyword_match integration test to cli_test.py</name>
  <files>src/cli_test.py</files>

  <read_first>
    src/cli_test.py — read the full file before editing; specifically read test_generate_called_with_analysis_dict_when_analysis_succeeds (the one test that mocks analyze_job_description to return a non-None dict) to understand whether it needs a @patch("cli.show_keyword_match") decorator to prevent unexpected behavior.
    src/cli.py — confirm the wired show_keyword_match call site matches what the new test should assert.
    .planning/phases/07-jd-keyword-match-summary/07-RESEARCH.md — Pitfall 4: the existing test that returns a non-None analysis dict may need @patch("cli.show_keyword_match") if it runs in a TTY environment.
  </read_first>

  <behavior>
    New test function `test_show_keyword_match_called_when_analysis_not_none`:
    - Decorators: @pytest.mark.unit, @patch("cli.analyze_job_description", return_value={"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}), @patch("sys.argv", ["resume-tailor"]), @patch("cli.show_keyword_match"), @patch("cli.show_diff"), @patch("cli.write_resume"), @patch("cli.generate_tailored_resume"), @patch("cli.read_resume"), @patch("builtins.input"), @patch("cli.run_guards").
    - mock_show_keyword_match must be a parameter (injected by @patch("cli.show_keyword_match")).
    - Asserts mock_show_keyword_match.assert_called_once_with({"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}, "\\documentclass{article}\n\\end{document}").

    Existing test test_generate_called_with_analysis_dict_when_analysis_succeeds:
    - Check whether it needs @patch("cli.show_keyword_match") added. In test environments sys.stdout.isatty() is False so show_keyword_match returns immediately — but if builtins.print is captured via side_effect, unexpected calls could corrupt printed_lines assertions in other tests. Add @patch("cli.show_keyword_match") to this test as a safety patch to prevent any side effects, and add the corresponding parameter mock_show_keyword_match to the function signature.

    No docstrings, no inline comments. Follow existing decorator ordering convention (reverse of parameter order).
  </behavior>

  <action>
    Edit src/cli_test.py in two locations:

    Location 1 — Add new test function `test_show_keyword_match_called_when_analysis_not_none` at the end of the file (before or after the last test — order does not matter for pytest discovery). The function body: sets up mock_input.side_effect = ["Software Engineer job", "END"], mock_read.return_value = "resume text", mock_generate.return_value = TailorResult(content="\\documentclass{article}\n\\end{document}", fences_stripped=False), mock_write.return_value = Path("/tmp/tailored_resume_test.tex"), calls main() inside `with patch("builtins.print"):`, asserts mock_show_keyword_match.assert_called_once_with({"technologies": ["Python"], "requirements": ["5 years"], "emphasis_areas": ["ML"]}, "\\documentclass{article}\n\\end{document}").

    Location 2 — Modify test_generate_called_with_analysis_dict_when_analysis_succeeds: add @patch("cli.show_keyword_match") decorator (place it immediately after @patch("cli.show_diff") since it follows the same pattern), and add mock_show_keyword_match parameter to the function signature in the correct position (reverse decorator order: it comes after mock_show_diff in the parameter list).

    Verify the decorator-to-parameter mapping is correct: unittest.mock stacks decorators such that the bottommost @patch decorator corresponds to the first mock parameter.
  </action>

  <verify>
    <automated>/workspace/.venv/bin/pytest -x -q</automated>
  </verify>

  <done>
    - src/cli_test.py contains a test function named test_show_keyword_match_called_when_analysis_not_none.
    - /workspace/.venv/bin/pytest -x -q exits 0 — all tests pass including the new one and the full prior suite.
    - grep -c "show_keyword_match" src/cli_test.py returns 2 or more (new test + existing test updated).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis dict -> cli.py -> show_keyword_match | The analysis dict (from jd_analyzer) flows through cli.py's None guard before reaching keyword_matcher.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | cli.py None guard for analysis | mitigate | Explicit `if analysis is not None:` guard in main() prevents AttributeError if analysis is None; show_keyword_match is never called with None |
| T-07-06 | Information Disclosure | show_keyword_match output visible in CLI stdout | accept | Output is intentional user-facing feedback; TTY-gated in keyword_matcher.py; no sensitive data in keyword list beyond what user typed as the JD |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib only |
</threat_model>

<verification>
After both tasks complete:
- grep -c "from keyword_matcher import show_keyword_match" src/cli.py returns 1
- grep -c "if analysis is not None:" src/cli.py returns 1
- grep -c "show_keyword_match" src/cli.py returns 2 (import line + call site)
- grep -c "show_keyword_match" src/cli_test.py returns 2 or more
- /workspace/.venv/bin/pytest -x -q exits 0 (full suite green)
- /workspace/.venv/bin/pytest src/cli_test.py -x -q exits 0
- /workspace/.venv/bin/pytest src/keyword_matcher_test.py -x -q exits 0
</verification>

<success_criteria>
- cli.py imports and calls show_keyword_match in the correct position (after show_diff, before final print, guarded by if analysis is not None)
- cli_test.py asserts show_keyword_match is called with the correct analysis dict and tailored content when analysis is not None
- All existing cli_test.py tests continue to pass unmodified (or with only the safety @patch("cli.show_keyword_match") addition to the one affected test)
- Full pytest suite (/workspace/.venv/bin/pytest -x -q) exits 0
</success_criteria>

<output>
Create .planning/phases/07-jd-keyword-match-summary/07-02-SUMMARY.md when done.
</output>
