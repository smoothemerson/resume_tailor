---
phase: "12-prompt-precision"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm_client.py
autonomous: true
requirements:
  - PRMP-01
  - PRMP-02
  - PRMP-03

must_haves:
  truths:
    - "System prompt contains an <ALLOWED> section listing title line, employer taglines, employer bullets, project subtitle, project bullets, and skills content as the only rewritable elements"
    - "System prompt contains a <CONSTRAINTS> section with a MUST NOT CHANGE list referencing concrete LaTeX inline patterns from the actual resume"
    - "System prompt contains a prominently labeled TECHNOLOGY FIDELITY rule inside <CONSTRAINTS> stating that absent technologies must not appear and present technologies must not be removed"
    - "All existing tests pass without modification"
  artifacts:
    - path: "src/llm_client.py"
      provides: "_build_messages() with rewritten system_prompt string"
      contains: "<ALLOWED>"
    - path: "src/llm_client.py"
      provides: "_build_messages() with rewritten system_prompt string"
      contains: "TECHNOLOGY FIDELITY"
  key_links:
    - from: "src/llm_client.py _build_messages()"
      to: "tests/unit/test_llm_client.py test_build_messages_system_contains_constraints_tag"
      via: "<CONSTRAINTS> and </CONSTRAINTS> tag literals"
      pattern: "<CONSTRAINTS>.*</CONSTRAINTS>"
---

<objective>
Rewrite the system_prompt string inside `_build_messages()` in `src/llm_client.py` to replace the generic `<INSTRUCTIONS>` / `<CONSTRAINTS>` content with explicit, concrete ALLOWED and MUST-NOT-CHANGE lists that reference the actual LaTeX inline patterns used in the resume.

Purpose: The current prompt describes what may change in vague terms ("rewrite skills section", "do not alter structural commands"). The new prompt names every changeable element by its LaTeX pattern and every protected element by its exact inline form. This reduces LLM ambiguity and mis-edits.

Output: Updated `src/llm_client.py` with a rewritten `system_prompt` string. No other file changes. All existing tests pass.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-prompt-precision/12-CONTEXT.md
@.planning/phases/12-prompt-precision/12-RESEARCH.md
@.planning/phases/12-prompt-precision/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _build_messages() system_prompt with ALLOWED/MUST-NOT-CHANGE/TECHNOLOGY FIDELITY sections</name>
  <files>src/llm_client.py</files>

  <read_first>
    - src/llm_client.py — read lines 26–121 to see current _build_messages() in full before editing; lines 27–95 are the system_prompt string (the only target); lines 96–121 are the user message assembly and must not be touched
    - tests/unit/test_llm_client.py — read to confirm all tag assertions; the binding test is test_build_messages_system_contains_constraints_tag (line 35) which asserts the literal strings "<CONSTRAINTS>" and "</CONSTRAINTS>" are present in result[0]["content"]
    - .planning/phases/12-prompt-precision/12-PATTERNS.md — contains the exact target content for <ALLOWED> and <CONSTRAINTS> sections including all LaTeX inline patterns extracted from the actual resume (§Pattern Assignments, §Sections being RENAMED/REWRITTEN)
  </read_first>

  <action>
    Replace only the system_prompt string content (lines 27–95 of src/llm_client.py). The function signature on line 26 and all code from line 96 onward are frozen — do not touch.

    Section order (per D-06): <PERSONA> -> <TASK> -> <CONTEXT> -> <ALLOWED> -> <CONSTRAINTS> -> <OUTPUT_FORMAT>

    Sections that do NOT change — copy verbatim from current file:
    - <PERSONA> block (current lines 28–34)
    - <TASK> block (current lines 36–45)
    - <CONTEXT> block (current lines 47–50)
    - <OUTPUT_FORMAT> block (current lines 83–94)

    Section renamed (D-01): Replace the <INSTRUCTIONS> open/close tags with <ALLOWED> open/close tags. Replace the numbered-list content with a bulleted ALLOWED list. The ALLOWED list must name these six elements exactly:
    1. Title line — the professional title in the contact header (may rewrite)
    2. Employer taglines — the \textit{\small ...}\\ line below each employer header (may rewrite)
    3. Employer bullet points — the \item entries inside \begin{itemize} under each employer; reword only, bullet count stays fixed
    4. Project subtitle — the descriptive text after \textbf{ProjectName} on each project line (may rewrite)
    5. Project bullet points — the \item entries inside \begin{itemize} under each project; reword only, bullet count stays fixed
    6. Skills content — the technology lists on \noindent\textbf{Category:} lines; reorder/reweight within categories, use only skills already present in the original
    Close the <ALLOWED> section with: "Everything not listed above must remain byte-for-byte identical."

    Section rewritten (D-02): Keep the outer <CONSTRAINTS> and </CONSTRAINTS> tags exactly as-is (this is the test-gated boundary). Replace the content inside with two labeled subsections:

    First subsection labeled "MUST NOT CHANGE:" as a bulleted list referencing these protected elements by their actual LaTeX inline patterns:
    - Candidate name: the {\Huge \scshape {Name}}\\ line inside the \begin{center} block
    - Contact block: the entire \begin{center}...\end{center} block at the top of the document (email, phone, location, LinkedIn, GitHub)
    - Education section: everything under \header{Education}
    - Languages section: everything under \header{Languages}
    - Employer header lines: company name, role title, location, and date range (pattern: \textbf{EMPLOYER}\textbf{ | ROLE} \hfill LOCATION\ $\cdot$\ DATES\\)
    - Project anchors: the \href{url}{\textbf{ProjectName}} and \hfill date on each project line
    - Section headers: all \header{...} commands
    - All LaTeX commands and environments: \documentclass, \usepackage, \newcommand definitions, \begin, \end, \vspace, \hfill, \textbf, \textit, \href, and all other structural commands
    - Bullet point count: do not add or remove \item entries in any list

    Second subsection labeled "TECHNOLOGY FIDELITY:" (per D-05) as a prose rule:
    Do not substitute one named technology for another. If a technology appears in the original resume it must appear in the output. If a technology is absent from the original resume it must not appear in the output, even if it appears in the job description. Include the illustrative example: if the resume mentions Azure, Azure must remain; if the resume does not mention AWS, AWS must not be added.

    Indentation: maintain the existing 8-space indentation convention throughout the string. The .strip() call at line 95 handles leading/trailing whitespace.

    Do NOT reference \employer{}{}{} or \contact{}{}{} macro-call signatures in the prompt — the actual resume body uses inline \textbf{...} patterns, not these macro calls (per RESEARCH.md §Pitfall 1 and §Exact Resume LaTeX Macro Inventory).
  </action>

  <verify>
    <automated>
      pytest tests/unit/test_llm_client.py -x -q
      pytest src/llm_client_test.py -x -q
      pytest -m unit -x -q
    </automated>
  </verify>

  <acceptance_criteria>
    - src/llm_client.py contains the literal string "<ALLOWED>" (tag added per D-01)
    - src/llm_client.py contains the literal string "</ALLOWED>"
    - src/llm_client.py contains the literal string "<CONSTRAINTS>" (tag kept per D-02)
    - src/llm_client.py contains the literal string "</CONSTRAINTS>"
    - src/llm_client.py contains the literal string "TECHNOLOGY FIDELITY" inside the <CONSTRAINTS> block (per D-05)
    - src/llm_client.py does NOT contain the literal string "<INSTRUCTIONS>" (renamed per D-01)
    - src/llm_client.py does NOT contain the literal string "\\employer{" in the system_prompt string (macro call pattern not used in resume body)
    - pytest tests/unit/test_llm_client.py::test_build_messages_system_contains_constraints_tag exits 0
    - pytest tests/unit/test_llm_client.py::test_build_messages_system_contains_persona_tag exits 0
    - pytest tests/unit/test_llm_client.py -x -q exits 0 (all unit tests in this file pass)
    - pytest src/llm_client_test.py -x -q exits 0 (all in-src tests pass)
    - The line count of _build_messages() function (from def _build_messages to the closing return statement) is unchanged or only differs in the string content region (lines 27–95); no code outside lines 27–95 is modified
  </acceptance_criteria>

  <done>
    _build_messages() system_prompt contains: (1) an <ALLOWED> tag listing exactly the six changeable elements with concrete LaTeX pattern references; (2) a <CONSTRAINTS> tag with a MUST NOT CHANGE bulleted list naming all nine protected elements by their actual inline LaTeX patterns; (3) a TECHNOLOGY FIDELITY labeled rule inside <CONSTRAINTS> with the example. All existing tests pass without modification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM prompt string -> Ollama API | The system prompt is a string literal sent to the local Ollama endpoint; it is not parsed by any runtime code in this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | system_prompt string in _build_messages() | accept | String literal in source-controlled Python file; no injection surface — the prompt is read by the LLM, not executed |
| T-12-02 | Information Disclosure | system_prompt content | accept | No PII or secrets in the prompt; Ollama is localhost-only; low-value target |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase — pure string edit, no dependency changes |
</threat_model>

<verification>
Run the full test suite from the project root after the task completes:

```
pytest -x -q
```

All tests must pass. The key tag-presence tests in `tests/unit/test_llm_client.py` (lines 28 and 35) assert `<PERSONA>` and `<CONSTRAINTS>` tags are present — both must remain.

Spot-check the prompt content manually:
- `grep -c '<ALLOWED>' src/llm_client.py` returns 1
- `grep -c '<CONSTRAINTS>' src/llm_client.py` returns 1
- `grep -c 'TECHNOLOGY FIDELITY' src/llm_client.py` returns 1
- `grep -v '^#' src/llm_client.py | grep -c '<INSTRUCTIONS>'` returns 0
</verification>

<success_criteria>
1. System prompt has a clearly labeled <ALLOWED> section listing exactly: title line, employer taglines, employer bullets, project subtitle, project bullets, skills reordering/reweighting
2. System prompt has a <CONSTRAINTS> section with a MUST NOT CHANGE subsection listing: candidate name, contact block, education section, languages section, employer header lines, project anchors, section headers, all LaTeX structural commands, bullet point count
3. System prompt includes a prominently labeled TECHNOLOGY FIDELITY rule inside <CONSTRAINTS> stating that present technologies must remain and absent technologies must not be added
4. All existing tests pass without modification (pytest -x -q exits 0)
</success_criteria>

<output>
Create `.planning/phases/12-prompt-precision/12-01-SUMMARY.md` when done.
</output>
