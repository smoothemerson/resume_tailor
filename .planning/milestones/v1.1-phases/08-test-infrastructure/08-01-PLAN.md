---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "pytest --co -q discovers all 18 existing tests in src/ without warnings or errors"
    - "pytest -m foo exits non-zero (exit 5) because no tests match the unregistered marker expression"
    - "pytest -m integration exits 0 even with no tests collected (SKIPPED or empty, not error)"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest configuration — testpaths, pythonpath, markers, addopts"
      contains: "[tool.pytest.ini_options]"
  key_links:
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "src/cli_test.py, src/llm_client_test.py"
      via: "testpaths = [\"src\", \"tests\"]"
      pattern: "testpaths.*src"
    - from: "pyproject.toml pythonpath"
      to: "src/ module imports in test files"
      via: "pythonpath = [\"src\"]"
      pattern: "pythonpath.*src"
---

<objective>
Add the [tool.pytest.ini_options] section to pyproject.toml, enabling the three test markers (unit, integration, e2e), strict-markers enforcement, and pythonpath-based import resolution so that sys.path hacks become redundant.

Purpose: Establishes the pytest configuration baseline that all downstream phases (9-11) depend on. Without this section, markers are unregistered, --strict-markers is not enforced, and import resolution relies on per-file hacks.
Output: pyproject.toml with [tool.pytest.ini_options] appended; pytest invocations immediately pick up the registered markers and testpaths.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/PROJECT.md
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/STATE.md
@/workspace/.planning/phases/08-test-infrastructure/08-CONTEXT.md
@/workspace/.planning/phases/08-test-infrastructure/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [tool.pytest.ini_options] to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — read the entire file to see current structure and insertion point; the new section appends after the [dependency-groups] block at line 27-32
    - .planning/phases/08-test-infrastructure/08-CONTEXT.md — D-05 specifies the exact config block to add; do not deviate from the keys or values shown there
    - .planning/phases/08-test-infrastructure/08-PATTERNS.md — "pyproject.toml — add [tool.pytest.ini_options] section" section shows the exact TOML to append
  </read_first>
  <action>
    Append the following section to the end of pyproject.toml, after the [dependency-groups] block. Do not insert between existing sections — append at end of file.

    The section to add (per D-05):

      [tool.pytest.ini_options]
      testpaths = ["src", "tests"]
      pythonpath = ["src"]
      markers = [
          "unit: fast isolated tests, no external deps",
          "integration: requires Ollama running locally",
          "e2e: full CLI subprocess invocation",
      ]
      addopts = "--strict-markers -ra"

    Key constraints:
    - testpaths must include both "src" and "tests" — the 18 existing tests live in src/
    - pythonpath = ["src"] is what makes the sys.path.insert hacks in test files redundant (D-02 cleanup happens in Plan 02)
    - addopts string "--strict-markers -ra" is a single space-separated string, not an array
    - All three marker strings must use the exact format: "name: description" (colon + space + description)
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest --co -q 2>&amp;1 | tail -5 &amp;&amp; echo "collect exit: $?" &amp;&amp; uv run pytest -m foo; echo "marker exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the section header [tool.pytest.ini_options]
    - pyproject.toml contains testpaths = ["src", "tests"]
    - pyproject.toml contains pythonpath = ["src"]
    - pyproject.toml contains addopts = "--strict-markers -ra"
    - pyproject.toml contains all three marker strings: "unit:", "integration:", "e2e:"
    - uv run pytest --co -q exits 0 and shows 18 tests collected with no warnings
    - uv run pytest -m foo exits non-zero (exit code 5 — no tests collected; the -m expression filters to nothing; --strict-markers fires on test-level decorators, not -m expressions)
  </acceptance_criteria>
  <done>pyproject.toml has [tool.pytest.ini_options] section with exact values from D-05; pytest --co -q collects 18 tests with exit 0 and no warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml → pytest | pytest reads ini options from pyproject.toml at startup; any value accepted without validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | pyproject.toml addopts | accept | addopts is a local dev config read from the repo; no external input; low-value target; any change is visible in git diff |
| T-08-02 | Information Disclosure | testpaths in pyproject.toml | accept | Exposes src/ layout; this is intentional and already public in the repo; no secrets involved |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; pytest 9.0.3 is an existing verified dev dependency |

ASVS L1 assessment: This plan modifies only a local configuration file. No network calls, no user input, no authentication, no data storage. Threat surface is negligible.
</threat_model>

<verification>
Run after plan completes:

1. uv run pytest --co -q — must exit 0, must show exactly 18 tests, must show no warnings
2. uv run pytest -m foo — must exit non-zero (exit 5)
3. uv run ruff check pyproject.toml — must exit 0 (ruff does not lint TOML but verifies no adjacent Python is broken)
4. grep -c "tool.pytest.ini_options" /workspace/pyproject.toml — must return 1
</verification>

<success_criteria>
TEST-01 satisfied: pyproject.toml has [tool.pytest.ini_options] with testpaths = ["src", "tests"], pythonpath = ["src"], all three markers registered, addopts = "--strict-markers -ra". pytest --co -q shows 18 tests with exit 0 and no warnings. pytest -m foo exits non-zero.
</success_criteria>

<output>
Create .planning/phases/08-test-infrastructure/08-01-SUMMARY.md when done
</output>
