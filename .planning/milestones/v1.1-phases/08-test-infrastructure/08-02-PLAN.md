---
phase: "08"
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - tests/conftest.py
  - src/cli_test.py
  - src/llm_client_test.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "tests/unit/, tests/integration/, and tests/e2e/ directories exist with no __init__.py files in them"
    - "tests/conftest.py provides an ollama_available fixture (session-scoped) that returns True/False via HTTP probe"
    - "tests/conftest.py provides a require_ollama fixture (function-scoped) that calls pytest.skip when ollama_available is False"
    - "uv run pytest -m integration exits 0 with all integration tests SKIPPED when Ollama is not running"
    - "src/cli_test.py has no sys.path.insert line; import sys and from pathlib import Path remain"
    - "src/llm_client_test.py has no sys.path.insert line and no import sys or from pathlib import Path lines"
    - "uv run pytest exits 0 with all 18 existing tests passing after sys.path cleanup"
  artifacts:
    - path: "tests/conftest.py"
      provides: "ollama_available and require_ollama pytest fixtures"
      exports: ["ollama_available", "require_ollama"]
    - path: "tests/unit/"
      provides: "empty directory for Phase 9 unit tests"
    - path: "tests/integration/"
      provides: "empty directory for Phase 10 integration tests"
    - path: "tests/e2e/"
      provides: "empty directory for Phase 11 e2e tests"
  key_links:
    - from: "tests/conftest.py"
      to: "src/config.py"
      via: "from config import OLLAMA_BASE_URL"
      pattern: "from config import OLLAMA_BASE_URL"
    - from: "tests/conftest.py ollama_available"
      to: "tests/conftest.py require_ollama"
      via: "require_ollama(ollama_available: bool) parameter injection"
      pattern: "def require_ollama.*ollama_available"
---

<objective>
Scaffold the tests/ directory hierarchy and create tests/conftest.py with the Ollama skip fixtures; then remove the now-redundant sys.path.insert hacks from both existing test files.

Purpose: Completes the test infrastructure so Phases 9-11 can add tests without any additional setup. The conftest.py fixtures let integration and e2e tests opt into skipping when Ollama is unavailable. The sys.path cleanup removes a confusing anti-pattern now that pythonpath = ["src"] handles import resolution.
Output: tests/ with three empty subdirectories (no __init__.py), tests/conftest.py with two fixtures, src/cli_test.py and src/llm_client_test.py with sys.path hacks removed.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/.planning/PROJECT.md
@/workspace/.planning/ROADMAP.md
@/workspace/.planning/STATE.md
@/workspace/.planning/phases/08-test-infrastructure/08-CONTEXT.md
@/workspace/.planning/phases/08-test-infrastructure/08-RESEARCH.md
@/workspace/.planning/phases/08-test-infrastructure/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/ scaffold and conftest.py fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>
    - src/config.py — confirms OLLAMA_BASE_URL is the exact constant name at line 5; conftest.py must import this constant, not hardcode the URL
    - .planning/phases/08-test-infrastructure/08-CONTEXT.md — D-03 (no __init__.py), D-04 (conftest.py is the only new Python file), D-06 (ollama_available is session-scoped), D-07 (import from config, use /api/tags endpoint, timeout=3), D-08 (require_ollama is function-scoped, calls pytest.skip)
    - .planning/phases/08-test-infrastructure/08-PATTERNS.md — "tests/conftest.py — new fixture file" section has the complete implementation and fixture design rules
    - .planning/phases/08-test-infrastructure/08-RESEARCH.md — Pattern 2 and the Code Examples section for the complete verified conftest.py contents
  </read_first>
  <action>
    Step 1: Create the three empty test subdirectories. No __init__.py in any directory (per D-03, rootdir-relative layout):
      /workspace/tests/unit/
      /workspace/tests/integration/
      /workspace/tests/e2e/

    Step 2: Create /workspace/tests/conftest.py with exactly:
      - Import: pytest, requests, OLLAMA_BASE_URL from config
      - ollama_available fixture: scope="session", performs requests.get to f"{OLLAMA_BASE_URL}/api/tags" with timeout=3, returns True on success, returns False on requests.ConnectionError; return type annotation bool
      - require_ollama fixture: no scope argument (defaults to "function"), parameter ollama_available: bool, calls pytest.skip("Ollama not available") when ollama_available is False; return type annotation None

    Critical constraints (all from locked decisions):
    - No __init__.py in tests/, tests/unit/, tests/integration/, or tests/e2e/ (D-03)
    - conftest.py is the ONLY new Python file in this phase (D-04)
    - ollama_available must use scope="session" (D-06)
    - Import OLLAMA_BASE_URL from config, never hardcode the URL (D-07)
    - Health endpoint: f"{OLLAMA_BASE_URL}/api/tags" with timeout=3 (D-07)
    - require_ollama must NOT use autouse=True — tests must opt-in by declaring the fixture parameter (D-08)
    - Catch only requests.ConnectionError in ollama_available, not bare Exception (per PATTERNS.md)
    - No inline comments or docstrings on functions (CLAUDE.md project convention)
    - Both fixtures have return type annotations: ollama_available() -> bool, require_ollama(...) -> None
  </action>
  <verify>
    <automated>ls /workspace/tests/unit /workspace/tests/integration /workspace/tests/e2e &amp;&amp; ls /workspace/tests/conftest.py &amp;&amp; grep -c "__init__" /workspace/tests/conftest.py || echo "no __init__ found (correct)" &amp;&amp; grep "OLLAMA_BASE_URL" /workspace/tests/conftest.py &amp;&amp; grep "scope=\"session\"" /workspace/tests/conftest.py &amp;&amp; grep "pytest.skip" /workspace/tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/, tests/integration/, tests/e2e/ directories exist
    - No __init__.py file exists in tests/, tests/unit/, tests/integration/, or tests/e2e/
    - tests/conftest.py exists
    - tests/conftest.py contains "from config import OLLAMA_BASE_URL" (not a hardcoded URL string)
    - tests/conftest.py contains scope="session" on the ollama_available fixture
    - tests/conftest.py contains "/api/tags" as the health probe path
    - tests/conftest.py contains timeout=3 in the requests.get call
    - tests/conftest.py contains pytest.skip("Ollama not available") in require_ollama
    - tests/conftest.py does NOT contain autouse=True on any fixture
    - tests/conftest.py does NOT contain any inline comments (# ...) or docstrings
    - uv run pytest -m integration exits 0 (Ollama not running means all integration tests SKIPPED — no tests collected yet so this is exit 5, which is also non-zero but acceptable since exit 0 only occurs when tests are present and skipped)
    - uv run ruff check /workspace/tests/conftest.py exits 0
  </acceptance_criteria>
  <done>tests/ scaffold created with three empty subdirectories (no __init__.py); tests/conftest.py has ollama_available (session-scoped) and require_ollama (function-scoped) fixtures; imports OLLAMA_BASE_URL from config; ruff passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sys.path.insert hacks from src/*_test.py</name>
  <files>src/cli_test.py, src/llm_client_test.py</files>
  <read_first>
    - src/cli_test.py — read the full file; line 6 is "sys.path.insert(0, str(Path(__file__).parent))" — remove this line only; keep "import sys" (line 1, used by @patch("sys.argv")) and "from pathlib import Path" (line 3, used by Path("/tmp/...") in mock return values)
    - src/llm_client_test.py — read the full file; remove three lines: line 1 "import sys", line 3 "from pathlib import Path", and line 6 "sys.path.insert(0, str(Path(__file__).parent))"; neither sys nor Path is used elsewhere in this file
    - .planning/phases/08-test-infrastructure/08-CONTEXT.md — D-01 (files stay in src/, not moved) and D-02 (sys.path.insert hacks removed)
    - .planning/phases/08-test-infrastructure/08-PATTERNS.md — "src/cli_test.py — remove sys.path.insert hack (line 6 only)" and "src/llm_client_test.py — remove sys.path hack AND unused imports (lines 1, 3, 6)" sections specify exact lines to remove per file
  </read_first>
  <action>
    Edit src/cli_test.py: remove exactly one line — the line reading "sys.path.insert(0, str(Path(__file__).parent))". Do NOT remove "import sys" or "from pathlib import Path" — both are used by tests in that file (sys for @patch("sys.argv") decorators, Path for mock return value Path("/tmp/tailored_resume_test.tex")).

    Edit src/llm_client_test.py: remove exactly three lines:
      1. "import sys" — not used anywhere else in the file
      2. "from pathlib import Path" — not used anywhere else in the file
      3. "sys.path.insert(0, str(Path(__file__).parent))" — the hack itself

    After removing these lines, the imports in llm_client_test.py should start with "import unittest".

    The pythonpath = ["src"] config added in Plan 01 (08-01-PLAN.md) makes these hacks redundant — pytest itself now adds src/ to sys.path before collecting tests.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; uv run ruff check src/cli_test.py src/llm_client_test.py &amp;&amp; uv run pytest src/ -q</automated>
  </verify>
  <acceptance_criteria>
    - src/cli_test.py does NOT contain the line "sys.path.insert"
    - src/cli_test.py still contains "import sys" on line 1
    - src/cli_test.py still contains "from pathlib import Path" on line 3
    - src/llm_client_test.py does NOT contain "sys.path.insert"
    - src/llm_client_test.py does NOT contain "import sys"
    - src/llm_client_test.py does NOT contain "from pathlib import Path"
    - src/llm_client_test.py first import line is "import unittest"
    - uv run ruff check src/cli_test.py exits 0 with no warnings
    - uv run ruff check src/llm_client_test.py exits 0 with no warnings
    - uv run pytest src/ -q exits 0 with all 18 tests passing
  </acceptance_criteria>
  <done>sys.path.insert removed from both test files; unused imports removed from llm_client_test.py only; ruff passes on both; all 18 tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/conftest.py → localhost:11434 | Single read-only HTTP GET to Ollama health endpoint; no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | ollama_available fixture | accept | Probes localhost only; reveals whether Ollama is running to the local user who invoked pytest — this is the intended behavior |
| T-08-04 | Denial of Service | ollama_available fixture hang | mitigate | timeout=3 on requests.get prevents the fixture from hanging the test session if Ollama is slow to start (per D-07 and RESEARCH.md Pitfall 5) |
| T-08-05 | Spoofing | OLLAMA_BASE_URL source | accept | URL imported from config.py (single source of truth); no user input path; attacker would need write access to config.py, at which point full repo access is assumed |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies (pytest, requests) are existing verified dev dependencies |

ASVS L1 assessment: The only external call is a read-only localhost HTTP GET with a 3-second timeout. No secrets, no user input, no data written to external systems. Threat surface is negligible for a local dev tool.
</threat_model>

<verification>
Run after plan completes:

1. uv run pytest --co -q — must exit 0, must show all 18 tests discovered (src/ tests), no warnings
2. uv run pytest -m integration — must exit 0 (no tests collected yet, or SKIPPED if any) — never exit with error
3. uv run pytest src/ -q — must exit 0, all 18 tests passing
4. uv run ruff check src/cli_test.py src/llm_client_test.py tests/conftest.py — must exit 0 for all three
5. ls tests/unit tests/integration tests/e2e — all three directories must exist
6. ls tests/unit/__init__.py tests/integration/__init__.py tests/e2e/__init__.py 2>&1 — must print "No such file or directory" for all (confirming no __init__.py)
7. grep "sys.path.insert" src/cli_test.py src/llm_client_test.py — must return no output (grep exits non-zero)
</verification>

<success_criteria>
TEST-02 satisfied: tests/conftest.py has session-scoped ollama_available fixture that probes OLLAMA_BASE_URL/api/tags with timeout=3 and returns bool; function-scoped require_ollama fixture that skips when ollama_available is False.
TEST-03 satisfied: tests/unit/, tests/integration/, tests/e2e/ directories exist with no __init__.py; src/*_test.py files remain in src/ per D-01.
D-02 satisfied: sys.path.insert removed from both src/cli_test.py (one line) and src/llm_client_test.py (three lines including unused imports); ruff clean on both files; all 18 existing tests pass.
</success_criteria>

<output>
Create .planning/phases/08-test-infrastructure/08-02-SUMMARY.md when done
</output>
