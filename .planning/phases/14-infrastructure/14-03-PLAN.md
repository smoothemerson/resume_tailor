---
phase: "14-infrastructure"
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - .gitignore
autonomous: true
requirements:
  - REPO-01
must_haves:
  truths:
    - ".gitignore contains .claude/ directory glob"
    - "git ls-files .claude/ returns empty output (nothing tracked)"
    - ".gitignore contains all standard Python ignore patterns from D-09"
  artifacts:
    - path: ".gitignore"
      provides: "Standard Python gitignore with .claude/ directory entry"
      contains: ".claude/"
  key_links:
    - from: ".gitignore"
      to: ".claude/"
      via: "directory glob prevents git from tracking .claude/ on subsequent git add"
      pattern: "^.claude/$"
---

<objective>
Replace .gitignore with a comprehensive standard Python ignore file (D-09) and remove .claude/ from git tracking so the directory is no longer versioned (REPO-01).

Purpose: .claude/ was committed accidentally and contains 429 tracked files. The current .gitignore only ignores .claude/settings.local.json — not the whole directory. This plan makes .claude/ permanently untracked while preserving all existing ignore patterns.

Output: Updated .gitignore with 9 categories of ignores; git index with .claude/ removed (429 files untracked); single commit combining both changes.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-infrastructure/14-CONTEXT.md
@.planning/phases/14-infrastructure/14-RESEARCH.md
@.planning/phases/14-infrastructure/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace .gitignore with standard Python ignore file (REPO-01, D-09)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore — read the current 5-line file to confirm existing entries before replacing: **/__pycache__, .venv, uv.lock, resumes/, .claude/settings.local.json. The new file must preserve resumes/ and uv.lock patterns.
    - .planning/phases/14-infrastructure/14-PATTERNS.md — .gitignore section (lines 244-276) has the verbatim target state and explains which existing entries are superseded vs preserved
    - .planning/phases/14-infrastructure/14-RESEARCH.md — Pitfall 5 documents the existing .gitignore content and replacement strategy
  </read_first>
  <action>
    Replace .gitignore entirely with the comprehensive standard Python gitignore per D-09. The new file must contain these sections and entries exactly:

    Section "# Python": __pycache__/, *.py[cod], *.egg-info/, dist/
    Section "# Virtual environments": .venv/
    Section "# Tools": .mypy_cache/, .ruff_cache/, .pytest_cache/
    Section "# Project-specific": resumes/, uv.lock
    Section "# Claude Code workspace": .claude/

    Notes on superseded entries: **/__pycache__ becomes __pycache__/ (standard form, more precise). .claude/settings.local.json is superseded by .claude/ directory glob (directory glob is broader, individual file entry is redundant and can be removed). resumes/ and uv.lock are preserved.

    CRITICAL: Write .gitignore BEFORE running git rm --cached in the next task. If .gitignore does not contain .claude/ before the git rm, a subsequent git add . could re-add the files (RESEARCH.md Pitfall 2).
  </action>
  <verify>
    <automated>grep -c "^\.claude/$" .gitignore && grep -c "^__pycache__/$" .gitignore && grep -c "^\.venv/$" .gitignore && grep -c "^resumes/$" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line ".claude/" (directory glob, not ".claude/settings.local.json")
    - .gitignore contains "__pycache__/" (standard form, not "**/__pycache__")
    - .gitignore contains ".venv/"
    - .gitignore contains "resumes/"
    - .gitignore contains "uv.lock"
    - .gitignore contains "dist/"
    - .gitignore contains "*.py[cod]"
    - .gitignore contains ".pytest_cache/"
    - .gitignore contains ".ruff_cache/"
    - .gitignore does NOT contain ".claude/settings.local.json" (the specific file entry is superseded by directory glob)
  </acceptance_criteria>
  <done>.gitignore contains .claude/ directory glob plus all 9 categories of standard Python ignores; resumes/ and uv.lock preserved</done>
</task>

<task type="auto">
  <name>Task 2: Remove .claude/ from git index and commit (REPO-01, D-10)</name>
  <files></files>
  <read_first>
    - .planning/phases/14-infrastructure/14-RESEARCH.md — Pattern 4 (lines 224-238) documents the exact git rm --cached command sequence and the pitfall of ordering; .gitignore must be written first (done in Task 1)
    - .planning/phases/14-infrastructure/14-CONTEXT.md — D-10 specifies the exact command: git rm -r --cached .claude/ then commit; local directory is preserved
  </read_first>
  <action>
    Execute git operations to remove .claude/ from the git index without deleting local files, then commit the .gitignore update and the index removal together in a single commit.

    Step 1: Verify .gitignore now contains .claude/ (from Task 1) — run: grep ".claude/" .gitignore
    Step 2: Remove .claude/ from git index (local files are preserved): git rm -r --cached .claude/
    Step 3: Stage the updated .gitignore: git add .gitignore
    Step 4: Verify staged state shows .gitignore modified and .claude/ files as deleted: git status
    Step 5: Commit both changes together: git commit -m "chore: untrack .claude/ and update .gitignore"

    CRITICAL: .gitignore must contain .claude/ BEFORE git rm --cached runs (already done in Task 1). The commit must include both .gitignore and the .claude/ deletion — do not split them into separate commits.
    CRITICAL: git rm -r --cached .claude/ preserves local files — the .claude/ directory will remain on disk but will no longer be tracked by git.
  </action>
  <verify>
    <automated>git ls-files .claude/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - git ls-files .claude/ returns empty output (wc -l returns 0 — nothing in .claude/ is tracked)
    - Local .claude/ directory still exists on disk (ls .claude/ returns files)
    - git log --oneline -1 shows the chore commit with both .gitignore and .claude/ deletions
    - git status shows no untracked .claude/ files (because .gitignore now covers the directory)
    - git show --stat HEAD shows .gitignore changed (1 file changed) + deleted: .claude/... entries (429 deletions from index)
  </acceptance_criteria>
  <done>git ls-files .claude/ returns 0 lines; local .claude/ directory intact; commit recorded with both .gitignore update and .claude/ index removal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index → working tree | git rm --cached modifies git index only; local files untouched |
| .gitignore → git tracking | .gitignore prevents re-tracking of .claude/ on future git add operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Information Disclosure | git history | accept | .claude/ will remain in git history before this commit; history rewriting (git filter-branch, BFG) is out of scope per REPO-01 spec which only requires "untracked from git history so the directory is no longer versioned" going forward |
| T-14-07 | Tampering | .gitignore ordering | mitigate | .gitignore is written in Task 1 before git rm --cached in Task 2 — prevents race condition where re-add could occur between removal and ignore entry |
| T-14-08 | Elevation of Privilege | .claude/ contents | accept | .claude/ contains Claude Code workspace files (settings, session data); these are local-only tooling artifacts with no credentials or secrets that require erasure from history |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages; no supply-chain risk |
</threat_model>

<verification>
After both tasks complete:
1. `grep ".claude/" .gitignore` outputs ".claude/" (directory glob present)
2. `git ls-files .claude/ | wc -l` returns 0 (nothing tracked)
3. `ls .claude/` lists files (local directory preserved)
4. `git status` shows clean working tree (no untracked .claude/ files due to .gitignore)
5. `grep "secrets\." .github/workflows/ci.yml` returns nothing (carry-forward from Plan 01 — confirm no secrets in CI)
</verification>

<success_criteria>
- .gitignore contains .claude/ directory glob plus comprehensive standard Python ignores (D-09)
- git ls-files .claude/ returns nothing — .claude/ directory is no longer tracked
- Local .claude/ directory is intact and accessible
- git status is clean after the commit
- All existing tests continue to pass: uv run pytest -m unit exits 0
</success_criteria>

<output>
Create `.planning/phases/14-infrastructure/14-03-SUMMARY.md` when done
</output>
